CONFIRMED FINDINGS (JSON):
[
  {
    "title": "Crash before /send delivery is reconciled as 'sent' (attempts set at claim-time, not send-time) â†’ invoice silently never mailed, money not collected",
    "severity": "P1",
    "category": "bug",
    "file": "apps/worker/src/lib/state-machine.ts",
    "lines": "560-574",
    "description": "reconcileInFlightSends treats `(row.attempts ?? 0) >= 1` as proof that a real send was already attempted, and therefore assumes the customer was emailed (the N-5 'bexio is_sent read-back quirk' tolerance). But `attempts` is set to 1 at CLAIM time in processOrder (line 219 `attempts: 1`), and the `transitionTo(...'sending')` at line 422 never bumps or re-checks it. So EVERY row that reaches status='sending' already has attempts>=1 BEFORE sendInvoice is ever called. If the worker is killed (Coolify OOM/redeploy, host kill, network partition) in the window between the 'sending' DB write (line 422) and sendInvoice actually delivering (line 425-430), the row is left status='sending', attempts=1, lock stale after 5 min. On the next run, getInvoice returns is_sent=false/undefined (genuinely never sent), and line 560 `attempts(1) >= 1` is TRUE â†’ the row is force-transitioned to 'sent' (line 570) with no email sent. The (order_id, billing_period) PK then permanently blocks any retry for that period.",
    "impact": "A real customer invoice is marked 'sent' in the DB and counted as sent in the Discord summary, but the email is never delivered and the invoice is never collected. This is silent revenue loss â€” exactly the failure the auditor flagged: 'mark unsent invoices as sent and skip a real customer email = money NOT collected'. It triggers on any crash in the brief send window, which on a daily-cron production bot is a realistic recurring risk (deploys, OOM, bexio timeouts mid-request).",
    "trigger": "Worker process is killed (redeploy / OOM / host kill) after the status='sending' write but before/during the sendInvoice POST, leaving a stale-lock 'sending' row with attempts=1. Next run's reconcileInFlightSends assumes-sent.",
    "suggestedFix": "Distinguish 'claimed' from 'send-attempted'. Bump attempts at the moment of the send attempt (in the 'sending' transition / immediately before sendInvoice), not at claim time â€” keep claim attempts at 0. Then the assume-sent branch only fires for rows that genuinely entered a send call at least once. Alternatively gate assume-sent on a separate `sendStartedAt` timestamp written right before sendInvoice, and treat attempts==1-from-claim as 'never actually called send â†’ safe to retry' (the existing else branch at 578).",
    "confidence": 0.82,
    "verifyNote": "Confirmed in current code. (1) processOrder inserts attempts:1 at CLAIM time (state-machine.ts:219); schema default is 0 (schema.ts:120), so this is an explicit override. (2) transitionTo(...'sending') at line 422 only sets status/updatedAt/lockAcquiredAt (helper at 482-499) â€” it never bumps attempts; confirmed by grep that attempts is only ever set to literal 1 at claim (219) or incremented +1 in retry paths (585, 612), never at send time. (3) In reconcileInFlightSends the branch order is: is_sent confirmed (553) â†’ attempts>=1 assume-sent (560) â†’ attempts>=MAX_ATTEMPTS (575) â†’ attempts===0 retry (578). Because claim forces attempts=1, every first-run crashed 'sending' row evaluates true at line 560 and is force-transitioned to 'sent', so the attempts===0 retry branch (whose comment at 579 says \"claimed but never actually called send. Safe to retry\") is dead for the order pipeline â€” the exact safe-retry case it was written for can never occur. Reachable trigger: worker killed (Coolify redeploy/OOM/host kill or unhandled crash) after the 'sending' DB commit (422) but before sendInvoice's single POST delivers (425-430, sendInvoice in invoices.ts:186-208 has no internal retry). After LOCK_STALE_MS=5min the row is reconciled: getInvoice returns genuinely is_sent=false, attempts(1)>=1 is TRUE â†’ marked 'sent' with no email, counted as reconciledSent in the summary, and the (order_id, billing_period) PK permanently blocks any retry for that period. No guard elsewhere distinguishes \"crash-before-send\" from the bexio read-back quirk (invoices.ts:182-184) because both leave attempts=1 + is_sent=false; the claim-time attempts assignment collapsed the two cases into one assume-sent verdict. Note: retryIssuedRows is irrelevant here (it only touches status='issued', not 'sending'). Severity P1 not P0: it is silent non-billing / un-retryable stuck period (money not collected), not double-billing or wrong-amount; it is probabilistic per hard-crash in a sub-second window rather than guaranteed per run, but the impact when it fires is exactly the audited nightmare (false 'sent' in DB + Discord, period permanently blocked).",
    "refinedFix": "Stop setting attempts at claim time and start counting actual send attempts. Concretely: change the claim INSERT (state-machine.ts:219) to attempts:0 (matching the schema default), and bump attempts as part of the transitionTo(...'sending') write at line 422 (e.g. add an attempts:+1 to that transition, applied both in processOrder and wherever a 'sending' transition is entered). Then a crash-before-send leaves attempts=0 â†’ reconcile routes it through the existing attempts===0 retry branch (578-590) which re-issues and retries the send, while a crash AFTER a genuine send leaves attempts>=1 â†’ assume-sent branch correctly tolerates bexio's read-back quirk. Re-order the reconcile branches so attempts>=MAX_ATTEMPTS (575) is checked BEFORE attempts>=1 (560), otherwise MAX_ATTEMPTS is unreachable. Equivalent alternative: add a sendStartedAt timestamp column written immediately before sendInvoice and gate the assume-sent branch on sendStartedAt != null instead of attempts>=1 (cleaner separation of \"claimed\" vs \"send-attempted\"). Either way, keep retryIssuedRows' +1 semantics consistent with the new counting model."
  },
  {
    "title": "Unreachable 'send_retries_exhausted' branch in reconcileInFlightSends (dead code masking retry-cap)",
    "severity": "P3",
    "category": "bug",
    "file": "apps/worker/src/lib/state-machine.ts",
    "lines": "575-577",
    "description": "The `else if ((row.attempts ?? 0) >= MAX_ATTEMPTS)` branch (line 575) can never execute: any row with attempts>=3 also satisfies the earlier `attempts >= 1` check at line 560, which captures it first and assumes-sent. Likewise the `attempts === 0 â†’ safe to retry` else branch (line 578-589) is effectively dead for rows reaching 'sending' from processOrder, because that path always sets attempts=1 at claim time. The branch ordering makes both the retry-cap and the safe-retry paths dead for the normal pipeline.",
    "impact": "There is no real per-invoice retry cap in crash-recovery: a 'sending' row is resolved to 'sent' on the very first reconcile pass rather than after MAX_ATTEMPTS real send attempts. This is the same root cause as the P1 above; on its own it is dead code / a hardening gap that hides intended retry semantics.",
    "trigger": "Any stale-lock 'sending' row (always attempts>=1) â€” the MAX_ATTEMPTS branch is structurally unreachable.",
    "suggestedFix": "Reorder/reframe the branches once attempts is bumped at send-time (see P1 fix): check is_sent â†’ then `attempts >= MAX_ATTEMPTS` â†’ then 'send-attempted but unconfirmed' assume-sent â†’ then 'never attempted â†’ retry'. The assume-sent tolerance must come AFTER the exhaustion check and AFTER confirming a real send was attempted.",
    "confidence": 0.85,
    "verifyNote": "Confirmed dead code, but the stated impact is overstated. CODE AS DESCRIBED: yes. In reconcileInFlightSends (state-machine.ts:551-590) the branches evaluate in order: (1) line 553 is_sent/mail_sent_at -> sent; (2) line 560 `attempts>=1` -> assume-sent (N-5); (3) line 575 `attempts>=MAX_ATTEMPTS(3)` -> send_retries_exhausted; (4) line 578 else (attempts===0) -> retry. UNREACHABILITY CONFIRMED: any row with attempts>=3 also satisfies attempts>=1, so the line-560 branch captures it first; line 575 can never execute. Every 'sending' row has attempts>=1 because the initial claim sets attempts:1 (line 219), transitionTo() (lines 482-499) never touches attempts when moving to 'sending' (line 422), and retryIssuedRows only bumps attempts higher (line 612). No path produces a 'sending' row with attempts===0, so the line-578 else is also dead for the normal pipeline. So branches 575-577 and 578-589 are genuinely unreachable -> real dead code. IMPACT OVERSTATED: the finding claims 'no real per-invoice retry cap in crash-recovery.' But reconcileInFlightSends is a confirm-or-assume reconciler, not a retrier â€” it never re-sends, so a 'retry cap' there is not load-bearing. The actual, working retry cap lives in retryIssuedRows (line 615: lt(attempts, MAX_ATTEMPTS)), which correctly stops retrying issued rows at 3 attempts. The assume-sent-on-first-pass behavior is the INTENTIONAL N-5 guard against double-emailing customers due to bexio's flaky is_sent read-back (documented lines 561-566, invoices.ts:182) â€” it is correct, not a bug. So the dead branches hide nothing that exists elsewhere and cause no money/double-billing/stuck-state harm. Net: real vestigial dead code (and a misleading retry-cap branch), severity P3 hardening/clarity â€” exactly as the finder rated it.",
    "refinedFix": "Delete the unreachable branches rather than reorder them, since reconcileInFlightSends is a confirm-or-assume reconciler that never re-sends. Keep only: (1) is_sent/mail_sent_at -> sent; (2) else -> assume-sent (N-5), since every reachable row here has attempts>=1 and an invoiceId. Drop the dead `attempts>=MAX_ATTEMPTS` and `attempts===0` arms (lines 575-589) and add a comment noting that the real retry cap is enforced in retryIssuedRows (lt(attempts, MAX_ATTEMPTS)). Do NOT add a retry-with-cap here â€” re-sending in the reconciler would reintroduce duplicate-email risk that N-5 deliberately avoids."
  },
  {
    "title": "Transient getInvoice failure during crash-recovery permanently marks an in-flight invoice 'failed'",
    "severity": "P2",
    "category": "edge-case",
    "file": "apps/worker/src/lib/state-machine.ts",
    "lines": "551-594",
    "description": "In reconcileInFlightSends, the only error handling around the bexio read-back is a single try/catch (line 551/591): if getInvoice(row.invoiceId) throws, the row is immediately markFailed (line 592). There is no retry or 'leave as sending for next run' path. While http.ts already retries transient errors internally, an exhausted-retry throw here (bexio 5xx window, token hiccup, network blip) will terminally fail a row whose invoice may in fact be issued and even sent in bexio.",
    "impact": "A recoverable transient read error during crash-recovery converts a stuck-but-valid 'sending' row into a terminal 'failed' row requiring manual review, even though re-reading on the next run would have resolved it cleanly. If the invoice was actually sent, the DB now disagrees with bexio (false failure); if it wasn't, the safe outcome is fine but still generates manual-review noise.",
    "trigger": "bexio GET /kb_invoice/{id} exhausts its internal retries (5xx / timeout) for a stale-lock 'sending' row during the reconcile loop.",
    "suggestedFix": "On a transient getInvoice failure, leave the row in 'sending' (do not markFailed) so the next scheduled run re-attempts reconciliation; only markFailed on a definitively permanent error (e.g. 404 invoice-not-found) or after a bounded number of reconcile passes.",
    "confidence": 0.85,
    "verifyNote": "Confirmed in apps/worker/src/lib/state-machine.ts:551-594. The getInvoice read-back is wrapped in a single try/catch; ANY throw from getInvoice (line 552) falls to the catch at 591-593, which unconditionally calls markFailed and increments reconciledFailed. There is no transient/permanent discrimination and no 'leave as sending' path. The finding is REAL and reachable. Crucially, the finding UNDERSTATES reachability: it assumes 'http.ts already retries transient errors internally', but http.ts/callBexio does NOT retry â€” its own comment (http.ts:51) states 'Caller is responsible for retry policy'. So a SINGLE transient 5xx (classifyStatus maps >=500 to 'transient', then throws BexioApiError), network blip, or token hiccup on the GET throws on first occurrence and terminally fails the row â€” no exhausted-retry sequence required. No guard neutralizes it: a 'failed' row is terminal. retryIssuedRows only claims status='issued' rows (state-machine.ts:615); reconcileInFlightSends only re-examines status='sending' rows (line 535). Once markFailed sets status='failed', no automated path re-examines that (orderId, billingPeriod) row, and the (orderId,billingPeriod) PK prevents a fresh row for the same period â€” manual review only. Severity P2 is correct: harm is incorrect-but-recoverable. The reconcile path only touches already-stale-lock 'sending' rows; the outcome is a false 'failed' (DB/bexio disagreement + manual-review noise) but NO double-send, wrong-amount, or wrong-recipient (markFailed does not re-send). It does not block future billing periods (each new period gets its own row) and it surfaces loudly as a failed row rather than silently, so it is not P1. Not P0/P1.",
    "refinedFix": "In the catch block (state-machine.ts:591-593), discriminate the error before failing. For a definitively permanent 404 (invoice-not-found) keep markFailed; for transient/auth/rate-limit/network errors, leave the row untouched in 'sending' (do NOT markFailed, do NOT clear lockAcquiredAt) so the next scheduled run re-runs reconcileInFlightSends against the still-stale lock. To avoid an infinite stuck loop, add a bounded reconcile-pass counter (e.g. reuse/extend a column or attempts) and only markFailed('reconcile_readback_unresolved') after N passes. Concretely: catch (err) { if (err instanceof BexioApiError && err.status === 404) { markFailed(...); reconciledFailed++; } else { console.warn('[reconcile] transient read-back failure, leaving sending for next run', err); /* leave row as-is */ } }. This mirrors the existing F-2 philosophy of rolling back to a re-pickable state on transient send failures rather than terminally failing."
  },
  {
    "title": "runSubscriptionNow has no due-date gate â€” repeated dashboard clicks bill not-yet-due periods immediately (early/double billing)",
    "severity": "P2",
    "category": "bug",
    "file": "apps/worker/src/lib/subscriptions.ts",
    "lines": "376-395",
    "description": "runSubscriptionNow loads the subscription and calls processOneSubscription with no check on status or nextBillingDate <= today (unlike processSubscriptions' loop guard at lines 136-140). processOneSubscription always uses sub.nextBillingDate as scheduledFor and, on success, advances next_billing_date by a full interval (lines 241-258). The (subscriptionId, scheduledFor) UNIQUE lock only dedupes within the SAME period. So a first click bills period D and advances to D+1 interval; a second click immediately bills period D+1 (a future, not-yet-due period) because scheduledFor is now different and no conflict occurs. The dashboard runNow (subscriptions/[id]/+page.server.ts:94-101) and billing-runs retry (billing-runs/+page.server.ts:30-41) actions expose this with no confirmation or due-check.",
    "impact": "Two real invoices created and auto-sent to the customer for two consecutive periods, the second one billed before it is due â€” customer is over-billed / billed early. Also fires for paused/cancelled subscriptions since status is never checked.",
    "trigger": "An operator double-clicking 'Jetzt abrechnen', or clicking retry on an already-succeeded run, or clicking runNow on a subscription that was already billed this cycle.",
    "suggestedFix": "In runSubscriptionNow, refuse when sub.status !== 'active' and when sub.nextBillingDate > today (return a 'not_due'/'skipped' result), so manual triggers cannot bill ahead of schedule or re-bill an already-advanced subscription.",
    "confidence": 0.8,
    "verifyNote": "Core mechanism confirmed in code. runSubscriptionNow (apps/worker/src/lib/subscriptions.ts:376-394) loads the sub and calls processOneSubscription with NO status check and NO nextBillingDate<=today check â€” unlike processSubscriptions which gates via SQL WHERE (lines 125-130) and the loop guard (lines 136-140). processOneSubscription derives scheduledFor = sub.nextBillingDate (line 162), and on success advances nextBillingDate by a full interval atomically (lines 241-258). The idempotency lock keys on (subscriptionId, scheduledFor) (line 173), so once the date advances the next manual run uses a NEW scheduledFor and does NOT conflict. Reachable harm: (1) a single 'Jetzt abrechnen' click on an active-but-not-yet-due subscription immediately creates and auto-sends a real invoice for a future period â€” the button is shown whenever status==='active' (subscriptions/[id]/+page.svelte:86-88) with no due-date check and no confirmation; (2) sequential clicks each bill one more future period (D, then D+interval, then D+2) because the lock no longer matches after each advance. With sub.autoSend && contact.mail (lines 227-235) these are really emailed to the customer = early/over-billing of real money. Two of the finder's specific sub-claims are REFUTED by the templates: the billing-runs Retry button only renders for status==='failed' (billing-runs/+page.svelte:76), so 'retry on an already-succeeded run' is not reachable via UI; and retrying a *failed* same-period run hits the still-present 'failed' billing_runs row (kept, not deleted, lines 289-296) â†’ UNIQUE conflict â†’ skipped_duplicate, so same-period re-bill is actually guarded. The paused/cancelled-via-button claim is also refuted (runNow button hidden for non-active status). But the central early-billing/repeat-future-billing path for ACTIVE subs is genuinely unguarded. Severity P2 is correct: it is real money/early-billing harm but requires an authenticated operator (behind Cloudflare Access) taking an intentional manual action; there is no automatic or external trigger, and the daily cron path is correctly gated. The suggested fix (refuse when status!=='active' or nextBillingDate>today) is the right direction; status!=='active' is defense-in-depth for the direct-POST path since the UI already hides the button.",
    "refinedFix": "In runSubscriptionNow, before calling processOneSubscription: (1) if sub.status !== 'active', return { kind:'failed', reason:'subscription not active' }; (2) compute today from the same UTC-midnight basis used elsewhere and if sub.nextBillingDate > today, return a new 'not_due'/'skipped' result instead of billing â€” this prevents manually billing future, not-yet-due periods and prevents sequential clicks from walking forward through future intervals. Keep the existing idempotency lock for same-period protection. Optionally add a confirm() on the 'Jetzt abrechnen' button, but the server-side due-gate is the load-bearing fix. Note: the billing-runs Retry path is already safe for same-period re-bills via the retained failed row + UNIQUE conflict, but it should still inherit the same status/due guard for direct-POST safety."
  },
  {
    "title": "Live billing form actions (runNow / retry / pause / cancel) lack origin-side CF Access verification",
    "severity": "P1",
    "category": "security",
    "file": "apps/web/src/routes/subscriptions/[id]/+page.server.ts",
    "lines": "94-101",
    "description": "The money-moving runNow action (and the billing-runs retry action at billing-runs/+page.server.ts:30-41) call runSubscriptionNow, which creates and sends real invoices. hooks.server.ts defines only handleError â€” there is NO `handle` hook â€” so verifyCfAccess (lib/server/cf-access.ts) is invoked only inside the three /api/* +server.ts endpoints, never for page-server form actions or load functions. These billing actions are therefore protected solely by Cloudflare Access at the edge, contradicting cf-access.ts's stated defense-in-depth goal that origin-direct requests without a CF-signed JWT should be 401.",
    "impact": "If the origin is reachable without going through Cloudflare (before the Hetzner firewall / CF-IP allowlist is enforced, which CLAUDE/cf-access.ts note is still pending), an unauthenticated request could invoke a billing action and cause a real invoice to be issued and emailed to a customer.",
    "trigger": "A direct POST to https://origin/subscriptions/{id}?/runNow that bypasses Cloudflare while the origin firewall lock is not yet in place.",
    "suggestedFix": "Add a `handle` hook in hooks.server.ts that runs verifyCfAccess for all non-public routes (or at minimum gate the runNow/retry/pause/cancel actions), so origin-side JWT verification covers the billing surface, not just /api/*.",
    "confidence": 0.85,
    "verifyNote": "Confirmed in current code. hooks.server.ts (lines 1-21) defines ONLY handleError; Grep finds no `export const handle` anywhere in apps/web, so SvelteKit runs no auth handle hook. verifyCfAccess is called only in the three /api/*/+server.ts endpoints (sync:18, runs/[id]:19, trigger-run:37) â€” never in any +page.server.ts load or action. The money-moving runNow action (subscriptions/[id]/+page.server.ts:94-101) and retry action (billing-runs/+page.server.ts:30-41) both invoke runSubscriptionNow -> processOneSubscription, the real invoice create+send path, with NO origin-side JWT check. The only thing protecting these is Cloudflare Access at the edge. The origin-side defense-in-depth that cf-access.ts (lines 8-11) was explicitly built to provide is wired up for /api/* but not for page actions. The Cloudflare Origin-Lock (Hetzner firewall / ufw CF-IP allowlist) that would otherwise block direct-origin requests is documented as a STILL-PENDING Phase-1 follow-up (DEPLOYMENT.md:214-235, HANDOFF_RECURRING_BOT.md:14, marked ðŸŸ¡ verbleibend). So today an attacker who learns the origin IP can POST /subscriptions/{id}?/runNow directly, bypass CF Access, and cause a real invoice to be issued and emailed to a customer. No guard elsewhere neutralizes this â€” I searched the whole web app. This is an auth bypass on a money-moving surface, but it is gated behind edge CF Access being in place and the origin IP being discovered, so per the severity calibration (\"exploitable-but-gated security issue\" = P1) it is P1, not P3. Raising from the finder's P3.",
    "refinedFix": "Add a `handle` hook in apps/web/src/hooks.server.ts that calls verifyCfAccess(event.request) for every non-public route (allow /health and any unauthenticated callback if needed), returning 401 on CfAccessError. This covers load functions AND form actions in one place, closing the page-action billing gap (runNow, retry, pause, cancel) the same way the /api/* endpoints are already covered. As belt-and-suspenders, prioritize completing the Cloudflare Origin-Lock (Hetzner firewall CF-IP allowlist per DEPLOYMENT.md:214-235) so the origin is never directly reachable."
  },
  {
    "title": "dry-run still sends real invoices via reconcileInFlightSends + retryIssuedRows",
    "severity": "P0",
    "category": "bug",
    "file": "apps/worker/src/lib/run.ts",
    "lines": "96-98",
    "description": "runDaily runs the two crash-recovery stages â€” reconcileInFlightSends (line 97) and retryIssuedRows (line 98) â€” BEFORE the dry-run gate and unconditionally, with no dryRun argument. retryIssuedRows (state-machine.ts L606-669) calls sendInvoice() at L639, which mails a real PDF to a real customer and transitions the row to 'sent'. reconcileInFlightSends also mutates invoice_runs rows to terminal 'sent'/'failed' states. Only the processOrder loop (L107) and syncRecurringOrders (L94) honor options.dryRun. So a run invoked with dryRun=true is NOT side-effect-free.",
    "impact": "A user (or Cowork via POST /api/trigger-run {dryRun:true}) running a 'dry run' to safely preview behavior will silently re-send real invoices for any order currently parked in 'issued' state, and permanently mutate invoice_runs rows to 'sent'. The 'issued' state is reached routinely: F-2 deliberately rolls a row back to 'issued' on any send failure so a later run retries it. So the trigger is realistic, not theoretical: any send hiccup leaves an 'issued' row, and the next 'dry-run' mails it.",
    "trigger": "Any invoice_runs row is in status='issued' (e.g. a prior send failed and F-2 rolled it back, or autoSend was toggled) AND someone invokes runDaily with dryRun=true (WORKER_DRY_RUN=true, bun run worker:dry, or POST /api/trigger-run {\"dryRun\":true}).",
    "suggestedFix": "Gate both crash-recovery calls on !options.dryRun, or thread options.dryRun into reconcileInFlightSends/retryIssuedRows so dry-run mode skips all bexio mutations (issue/send) and DB writes. Simplest: `if (!options.dryRun) { reconcile = await reconcileInFlightSends(...); retriedFromIssued = await retryIssuedRows(...); }` and default the counters to 0.",
    "confidence": 0.95,
    "verifyNote": "Confirmed real and reachable in current code. run.ts:97-98 calls reconcileInFlightSends(db, accessToken) and retryIssuedRows(db, accessToken, settings) unconditionally, with NO dryRun parameter, BEFORE the dry-run gate at run.ts:107. retryIssuedRows (state-machine.ts:606-669) atomically claims every invoice_runs row in status='issued' (attempts < MAX_ATTEMPTS) and at line 639 calls sendInvoice(..., {attachPdf:true}) to order.customerEmail â€” a real PDF email to a real customer â€” then transitions the row to 'sent'. There is no dryRun check anywhere in that function or in reconcileInFlightSends (which also permanently mutates rows to terminal sent/failed/issued states). Both trigger paths are verified: (1) HTTP â€” trigger-run/+server.ts:40 sets dryRun = body.dryRun === true and line 92 threads it straight into runDaily, so any CF-Access-authenticated caller (e.g. Cowork) POSTing {\"dryRun\":true} hits the unguarded stages; (2) CLI â€” package.json:14 + cli.ts:18,63 run runDaily with dryRun from WORKER_DRY_RUN=true. The 'issued' state is routinely reachable: F-2 (state-machine.ts:445-456) deliberately rolls a row back to status='issued' on any send failure so a later run retries â€” exactly the parked-row precondition. By contrast syncRecurringOrders (run.ts:94) and the processOrder loop (run.ts:107) both honor dryRun, so the inconsistency is real, not an intended design where dry-run still recovers. No guard elsewhere neutralizes this: the loop's dryRun branch at 107 is reached AFTER 97-98 already executed. Net: a 'dry run' is not side-effect-free â€” it can silently mail a real invoice and permanently mutate invoice_runs. That is squarely the P0 category (sending a real invoice when none intended). One refinement: onlyOrderId is also not threaded into these stages, so even a single-order dry-run reprocesses every org-wide 'issued' row, slightly widening blast radius beyond the finding's claim.",
    "refinedFix": "Gate both crash-recovery calls on !options.dryRun and default the counters so the summary stays well-formed: e.g. `let reconcile = { reconciledSent: 0, reconciledIssued: 0, reconciledFailed: 0 }; let retriedFromIssued = 0; if (!options.dryRun) { reconcile = await reconcileInFlightSends(db, accessToken); retriedFromIssued = await retryIssuedRows(db, accessToken, settings); }`. Optionally also scope these stages by onlyOrderId when set, so a targeted live-test run does not reprocess unrelated 'issued' rows org-wide. Longer term, thread an explicit dryRun flag into both functions so the dry-run contract is enforced at the function boundary rather than only at the call site."
  },
  {
    "title": "Cron daily run has no in-flight guard â€” can run concurrently with a Cowork-triggered run",
    "severity": "P2",
    "category": "edge-case",
    "file": "apps/worker/src/lib/run.ts",
    "lines": "75-87",
    "description": "The HTTP trigger endpoint (apps/web/.../trigger-run/+server.ts L48-75) guards against overlap by checking for a bot_runs row with finished_at IS NULL younger than STALE_MS and returning 409. The CLI/cron entry path in runDaily simply inserts a fresh bot_runs row (L78-86) with no such check. So if a Cowork-triggered run (or a previous cron run on a slow bexio rate-limit) is still in flight when the 06:00 UTC cron fires, two runDaily executions proceed in parallel.",
    "impact": "Two concurrent runs race on the same enabled orders. The per-order claim (invoice_runs PK INSERT ON CONFLICT, state-machine.ts L213-244) and atomic retryIssuedRows UPDATE...RETURNING mostly prevent double-billing, so this is not P0/P1. But it doubles bexio API load against the rate limiter, can produce confusing duplicate bot_runs summaries/notifications, and leans entirely on the lower-level locks rather than an orchestration-level guard.",
    "trigger": "A Cowork or manual run started within the last 2h is still executing (large order count Ã— ~1.1s bexio pacing) when the daily cron fires, or two cron invocations overlap after a slow run.",
    "suggestedFix": "Add the same in-flight check used by trigger-run to the cron path (either inside runDaily when existingRunId is absent, or in cli.ts before calling runDaily): query for an unfinished bot_runs row younger than the stale window and skip/abort if found.",
    "confidence": 0.82,
    "verifyNote": "Code matches the finding exactly. CLI/cron path: DEPLOYMENT.md confirms the Coolify daily-run task at 0 6 * * * UTC executes `docker exec ... bun run apps/worker/src/cli.ts`, and cli.ts:63 calls `runDaily(db, { dryRun, onlyOrderId })` with NO existingRunId. run.ts:75-87 then takes the `else` branch and inserts a fresh bot_runs row with zero in-flight check. By contrast, trigger-run/+server.ts:49-75 DOES guard: it selects the newest bot_runs row WHERE finished_at IS NULL and returns 409 if age < STALE_MS (default 2h). The guard is one-directional â€” it protects the HTTP path from itself/prior runs, but the cron path checks nothing, so cron never yields to an in-flight Cowork/manual run nor to a slow prior cron run. The overlap path is reachable: a Cowork/manual /api/trigger-run still in flight within the 2h stale window (large order count x ~1.1s bexio pacing) when 06:00 cron fires â†’ two runDaily executions run in parallel with no orchestration-level mutual exclusion. Severity is correctly P2, NOT P0/P1: the per-order claim in state-machine.ts:213-244 is period-keyed and atomic (INSERT ... onConflictDoNothing on [orderId, billingPeriod], with explicit duplicate / concurrent-in-flight handling), and retryIssuedRows uses atomic UPDATE...RETURNING. These genuinely prevent double-billing/double-send across concurrent runs. Residual harm is bounded and recoverable: doubled bexio API load against the rate limiter, duplicate/confusing bot_runs rows and Discord notifications, and total reliance on low-level locks rather than an orchestration guard â€” observability/robustness, not money. Confidence held below 0.9 because the overlap requires a manual/Cowork trigger or an unusually slow run to coincide with the daily cron, which is plausible but not an everyday occurrence.",
    "refinedFix": "Add a run-level guard, but prefer a real lock over a racy SELECT. Putting the trigger-run in-flight SELECT into runDaily's else-branch (or cli.ts before calling runDaily) still has a check-then-insert TOCTOU (both processes can pass the SELECT before either INSERTs) â€” the same theoretical race already noted for the HTTP path in the plan file (L647). Since per-order period locks already prevent the money harm, a best-effort SELECT guard is acceptable as a quick win, but the robust fix is to wrap the whole run in a Postgres session/transaction advisory lock (pg_try_advisory_lock with a fixed key) â€” matching the F-6 token-refresh pattern â€” and abort/skip the run if the lock is already held. That gives true mutual exclusion across both the cron CLI path and the HTTP path with one mechanism."
  },
  {
    "title": "Unsupported-interval orders surfaced by sync are dropped by the orchestrator",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/worker/src/lib/run.ts",
    "lines": "94, 165-200",
    "description": "syncRecurringOrders returns unsupportedOrders (sync.ts SyncResult, populated at sync.ts L196-198 / L247-249) â€” orders whose bexio repetition.type the bot cannot process (e.g. custom intervals, or 'supported type but unparseable schedule'). runDaily captures the sync result at L94 but never reads sync.unsupportedOrders: it is not written to the bot_runs row and not passed into notifyAll (L165-200 only forwards sync.newOrders and sync.driftWarnings). The CLI --sync mode logs drift warnings (cli.ts L48) but also never logs unsupportedOrders.",
    "impact": "An order that bexio holds as recurring but the bot silently cannot bill (unsupported/unparseable schedule) produces no operator-visible signal from the daily run â€” no notification, no bot_runs note. Marcus may believe a customer is being billed when the bot is silently skipping them, delaying detection of missed revenue until a customer complains. Recoverable but an observability gap.",
    "trigger": "A recurring order in bexio uses an interval type the bot maps as unsupported (custom, or weekly without weekdays), and the operator relies on the daily Discord summary / dashboard to know billing coverage.",
    "suggestedFix": "Thread sync.unsupportedOrders into the notifyAll payload and/or the bot_runs errors/notes so unsupported orders surface every run, mirroring how driftWarnings are already forwarded.",
    "confidence": 0.8,
    "verifyNote": "Code matches the finding. sync.ts populates SyncResult.unsupportedOrders for (a) unsupported repetition.type (L89-90, pushed L197/L248) and (b) supported-type-but-unparseable schedules (L91-95, the \"(Konfiguration unklar)\" case). run.ts L94 captures the sync result as `sync` but L165-200 forwards only sync.newOrders and sync.driftWarnings into notifyAll; sync.unsupportedOrders is never read, never written to the bot_runs row. The notify contract confirms this is unreachable downstream: DiscordRunReport (discord.ts L42-44) has fields for newOrders and driftWarnings but NO unsupportedOrders field. The CLI --sync path (cli.ts L47-48) logs newOrders and driftWarnings only. So the sync-side unsupported flag has no surfacing path in the recurring run.\n\nI checked the obvious guard: the state-machine `skipped_unsupported` result kind IS surfaced (run.ts L187, discord.ts L143-149, cli.ts L102-104). But that is a DISTINCT signal that only fires for enabled orders processed by getEnabledOrders (enabled=true filter, sync.ts L371-381). A sync-discovered unsupported order lands enabled=false (sync never auto-enables), so it is never processed by the state machine and produces no skipped_unsupported result. That order therefore yields zero operator-visible signal from the daily run â€” confirming the gap is real and not neutralized by the state-machine path.\n\nSeverity is correctly P3, not higher: an unsupported order is enabled=false by default, so the bot was never going to bill it â€” this is not a silent failure of an ACTIVE billing path and there is no money loss or double-billing. There is also a partial mitigation: the web /orders/import page surfaces unsupportedOrders.length at manual-import time (+page.svelte L86). The remaining gap is purely the recurring daily-summary / dashboard observability for orders bexio adds as recurring-but-unsupported after import. Recoverable, detection-delay only. P3 stands.",
    "refinedFix": "Thread sync.unsupportedOrders through to operator-visible surfaces, mirroring driftWarnings: (1) add an optional `unsupportedOrders` field to DiscordRunReport (discord.ts ~L44) and render a \"âš  Nicht unterstÃ¼tzt\" embed field similar to the driftWarnings block at discord.ts L187-197; (2) pass `unsupportedOrders: sync.unsupportedOrders` in the notifyAll payload at run.ts L165-200; (3) optionally persist a note/count on the bot_runs row so the dashboard can show it; (4) add a `for (const u of result.unsupportedOrders) console.log(...)` line in cli.ts after L48 in --sync mode. Note this only matters for orders that are recurring in bexio but unsupported and not yet enabled â€” keep severity low."
  },
  {
    "title": "FULL-sync orphan cleanup deletes every order past the silent 5000 pagination cap",
    "severity": "P1",
    "category": "edge-case",
    "file": "apps/worker/src/lib/sync.ts",
    "lines": "163-171, 254-268",
    "description": "In FULL mode `orders = await listRecurringOrders(accessToken)` (sync.ts:170), and that client (packages/bexio-client/src/orders.ts:28-34) silently truncates at offset 5000 by breaking the pagination loop and returning a PARTIAL list â€” it does NOT throw. `seenIds` is then built only from that truncated set, and the orphan-cleanup at sync.ts:263-266 runs `db.delete(recurringOrders).where(notInArray(bexioOrderId, seenArray))`, which hard-DELETEs every cached row whose order id was beyond the cap. The existing F-7 guard only protects the `length === 0` case (sync.ts:257), not the truncated-but-nonempty case.",
    "impact": "Once the bexio account exceeds ~5000 orders, every legitimately-tracked recurring order past the cap (including enabled, opted-in, billing rows) is permanently deleted from the cache on the next full sync. Those customers silently stop being billed â€” a stuck/non-billing state with no error surfaced.",
    "trigger": "bexio account grows past 5000 orders (the cap the client itself logs as a 'safety' degrade), then a full sync runs.",
    "suggestedFix": "Make listRecurringOrders signal truncation (return a flag or throw on cap-hit), and in syncRecurringOrders skip orphan cleanup whenever the source list was truncated â€” same fail-safe philosophy as the F-7 empty-list guard. Only delete when the bexio list is known-complete.",
    "confidence": 0.85,
    "verifyNote": "Confirmed in current code. (1) FULL sync (the default mode, sync.ts:156) is what the daily orchestrator runs: run.ts:94 calls syncRecurringOrders with no mode. (2) FULL mode calls listRecurringOrders (sync.ts:170), which on cap-hit logs console.warn and `break`s, returning a PARTIAL list with NO throw and NO truncation signal (orders.ts:31-34). (3) seenIds is built only from that returned set (sync.ts:178,184). (4) Orphan cleanup runs a hard db.delete on notInArray(bexioOrderId, seenArray) (sync.ts:263-266). (5) The F-7 guard only short-circuits the seenArray.length===0 case (sync.ts:257); a truncated-but-nonempty list (~5200 rows) passes straight into the DELETE. So every cached recurring order whose id sorts beyond the cap â€” including enabled=true opt-in billing rows â€” is permanently deleted. Re-sync does NOT heal this: auto-discovery always re-inserts with enabled=false (sync.ts:216), so the opt-in flag that drives billing is lost for good, and getEnabledOrders (sync.ts:371-381) then skips those customers â€” silent non-billing, no error surfaced (removedOrders is only an informational count). One reachability sharpening the finder understated: the 5000 cap is on the RAW /kb_order pagination, BEFORE the client-side is_recurring filter (orders.ts:23), so the trigger is ~5200 TOTAL orders of any type (one-off invoices/quotes included), not 5000 recurring orders â€” a far more reachable threshold that accrues over years of normal invoicing, and bexio's offset order isn't guaranteed stable so which rows get dropped can churn. Not P0 (no wrong-amount/mis-sent invoice; money isn't moved to the wrong place), but it is data-loss in the DB cache plus a stuck state that blocks future billing â€” squarely the P1 class. Severity P1 is correct.",
    "refinedFix": "Make listRecurringOrders (and listRecurringOrdersSince) signal incompleteness instead of silently returning a partial list â€” e.g. return { orders, truncated: boolean } or throw a dedicated TruncatedListError on cap-hit. In syncRecurringOrders, thread that flag through and gate the orphan-cleanup DELETE on it: skip cleanup whenever the source list was truncated, exactly mirroring the F-7 length===0 fail-safe, and emit a loud warning/notification so the unbounded-growth condition surfaces. Only DELETE when the bexio list is known-complete. Additionally, since the 5000 cap counts raw orders pre-is_recurring-filter, consider raising the cap or paginating with a server-side is_recurring filter (POST /kb_order/search is noted to support it per memory 35594) so the recurring set is fetched completely without scanning all historical one-off orders."
  },
  {
    "title": "A single malformed order aborts the entire sync (and the whole daily billing run)",
    "severity": "P2",
    "category": "edge-case",
    "file": "apps/worker/src/lib/sync.ts",
    "lines": "183-250, 205-237",
    "description": "The per-order loop has no per-iteration try/catch. `expectedAmount: o.total` (sync.ts:215/228) is written straight into a NOT NULL text column (schema.ts:86); the BexioOrder.total type is non-optional but the value originates from raw bexio JSON (and from POST /kb_order/search results in incremental mode). If any order in the page has a null/undefined `total`, an unexpected status, or the insert otherwise violates a constraint, the await throws and propagates out of the loop. Because run.ts:94 calls syncRecurringOrders WITHOUT a surrounding try/catch (unlike the per-order processing loop at run.ts:116 which IS guarded), the throw aborts runDaily before any enabled order is billed.",
    "impact": "One bad/edge-case order from bexio takes down the whole daily cycle: no invoices created or sent that day, and in incremental mode the watermark is never advanced (good) but in full mode orphan cleanup is also skipped. A money-blocking, hard-to-diagnose outage from a single odd record.",
    "trigger": "bexio returns one order with a null `total` (e.g. a draft/zeroed order, an API schema change, or a malformed search row), or any DB constraint hiccup on one row.",
    "suggestedFix": "Wrap the body of the for-loop in try/catch, push the failing order id into a 'failed' bucket in SyncResult, and continue. Separately, validate/guard `o.total` before the insert and skip-with-warning if it is null. Also wrap the syncRecurringOrders call in run.ts so a sync failure degrades the run rather than killing it.",
    "confidence": 0.72,
    "verifyNote": "Code structure confirmed exactly as described. sync.ts:183-250 has no per-iteration try/catch; the per-order INSERT (sync.ts:205-237) is an unguarded await. expectedAmount is NOT NULL text (schema.ts:86) and o.total is written straight in (sync.ts:214,228). BexioOrder.total (types.ts:216) is a compile-time-only string type â€” callBexio returns raw parsed JSON with no validation/coercion, so a null/undefined total is structurally possible. run.ts:94 calls syncRecurringOrders with NO surrounding try/catch, and runDaily has no top-level catch, so a throw propagates out before the (guarded) order-processing loop at run.ts:106 ever runs. The CLI try/catch (cli.ts:23-132) only logs FATAL + sets exit code; it does not recover the run. Net: a single failing order = zero invoices billed that day. Reachable and real. Two caveats lower confidence/severity from the finder's framing: (1) the *null-total* trigger specifically is somewhat speculative â€” recurring orders in bexio always carry a computed total string, so this needs an API contract break or genuinely malformed row, not a routine event; (2) the more defensible harm is the general lack of per-iteration resilience â€” ANY single INSERT failure (transient DB deadlock/connection blip, a real NOT NULL violation) aborts the whole cycle. Notably buildOrderRow ALREADY swallows per-order repetition (82-101) and contact (104-116) errors, proving the authors intended per-order resilience but left the INSERT unguarded â€” a genuine gap, not a guarded non-issue. The orphan-cleanup-skipped point is minor and actually protective (no data loss). Fails closed (no wrong/double invoice â€” money-safe) and throws loudly (no bot_runs close, Discord/canary absence surfaces within 24h), so P2 not P1; not P0/P1 because no money is mis-sent and detection is fast.",
    "refinedFix": "Wrap the for-loop body (sync.ts:183-250) in try/catch: on error, push {bexioOrderId: o.id} into a new `failedOrders` bucket on SyncResult and `continue`, mirroring the per-order guard the authors already use in run.ts:116 and inside buildOrderRow. Additionally coerce/guard o.total before the insert â€” e.g. `const amount = (o.total ?? '0').toString()` or skip-with-warning if total is null/undefined â€” so a malformed field can never hit the NOT NULL column. Surface the failed bucket in the notification so a degraded sync is visible. Optionally also wrap the run.ts:94 syncRecurringOrders call so a total sync failure degrades the run (still attempts to bill already-cached enabled orders) rather than aborting runDaily entirely."
  },
  {
    "title": "parseWindowDays accepts fractional and unbounded due-window values from the settings page",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/worker/src/lib/settings.ts",
    "lines": "58-61",
    "description": "parseWindowDays only checks Number.isFinite(n) && n >= 0. It accepts fractional values (Number('2.5') -> 2.5) and arbitrarily large values (Number('100000') -> 100000) with no upper bound and no integer coercion. The value flows into isOrderDue's windowDays and is compared as diffDays <= windowDays where diffDays is an integer (next-billing.ts L285-288). A fractional 2.5 silently behaves like 2 (confusing but harmless); a huge value turns the 'catch-up tolerance' into a back-bill window: an order whose most recent scheduled occurrence is, say, 90 days in the past would be billed today if order_due_window_days were set to 100 via the unvalidated /settings text field. The dueWindowDays default elsewhere in the file is described as a small catch-up tolerance (3 days).",
    "impact": "An operator (or a fat-fingered settings save) entering a large order_due_window_days turns the due-gate into a retroactive biller: the next cron run creates+sends a real invoice for an old occurrence the system intentionally never back-bills. With autoSend on, that is an unexpected real email + invoice to a customer.",
    "trigger": "Someone sets order_due_window_days to a large number (or a non-integer) on the /settings page; the worker loads it on the next run and isOrderDue treats any occurrence within that many days as due.",
    "suggestedFix": "Coerce to a non-negative integer (Math.floor) and clamp to a sane maximum (e.g. <= 31). Validate the input on the /settings form too, and reject values that imply back-billing beyond one interval.",
    "confidence": 0.7,
    "verifyNote": "Partially confirmed, partially refuted. Code at apps/worker/src/lib/settings.ts:58-61 is exactly as quoted: parseWindowDays only checks Number.isFinite(n) && n >= 0, with no integer coercion and no upper bound. Data flow confirmed: dueWindowDays -> state-machine.ts:184 -> isOrderDue(rep, now, windowDays) -> next-billing.ts:288 returns the occurrence when diffDays <= windowDays. computeCurrentOccurrence (next-billing.ts:204-255) returns the single latest occurrence on/before now, and the docstring (next-billing.ts:264-271) explicitly states the design deliberately does NOT back-bill older periods. So a large windowDays turns the catch-up tolerance into a retroactive biller for an occurrence up to windowDays days old â€” with autoSend on (default true) that means a real invoice created+issued+sent.\\n\\nHOWEVER the finding mischaracterizes the input path. The /settings web action IS validated: apps/web/src/routes/settings/+page.server.ts:125-138 does `Number.isInteger(due) || due < 0` -> fail(400). Therefore the finding's FRACTIONAL claim (2.5 -> 2.5) is REFUTED: a non-integer cannot be persisted via the form, and parseWindowDays is never reached with a fractional string from any reachable source. The finding's claim that the field is an 'unvalidated /settings text field' is also inaccurate â€” it is validated for integer and non-negativity. What is genuinely missing in BOTH the form and parseWindowDays is an UPPER BOUND: Number.isInteger(100) && 100 >= 0 passes the form, is persisted as '100', and parseWindowDays passes it through. The svelte input (line 108) has type=number min=0 step=1 but no max (and client constraints are bypassable via raw POST anyway). So the large-value back-bill path IS real and reachable.\\n\\nSeverity P3 is correct: trigger requires an authenticated operator (single trusted user behind Cloudflare Access OTP) to deliberately or accidentally enter a large integer into a config field. Not externally triggerable, not a silent automatic failure â€” a defense-in-depth / input-hardening gap on a trusted-operator config value. The real harm (an unintended back-bill invoice) keeps it from being lower, but the privileged-misconfiguration trigger keeps it from being higher.",
    "refinedFix": "Add an upper-bound clamp in BOTH places (the worker fallback parseWindowDays and the authoritative web form), since the form is the real entry point. In apps/web/src/routes/settings/+page.server.ts after line 136, also reject due > a sane max (e.g. 31) with a clear message that values beyond one interval would back-bill. In apps/worker/src/lib/settings.ts:58-61, defensively coerce: `const n = Math.floor(Number(raw ?? '3')); return Number.isFinite(n) && n >= 0 ? Math.min(n, 31) : 3;` so any stale/out-of-band DB value is also clamped. Drop the fractional concern from the fix â€” Number.isInteger at the form already prevents it. Optionally add max=31 to the svelte input (line 108) for UX, but rely on the server check as authoritative."
  },
  {
    "title": "Token-refresh holds pg advisory lock + pooled DB connection across an un-timed network fetch",
    "severity": "P1",
    "category": "bug",
    "file": "packages/bexio-client/src/auth.ts",
    "lines": "63-108",
    "description": "getValidAccessToken() opens a db.transaction, acquires pg_advisory_xact_lock(4242001) (line 64), then calls callTokenEndpoint(params) (line 93) â€” which in http.ts:100-107 does `await pace()` (up to 1.1s) and then a bare `fetch(...)` to https://auth.bexio.com with NO timeout / AbortSignal. The advisory lock is xact-scoped, so it (and the underlying pooled Postgres connection from a `max:5` pool with no statement_timeout) is held for the entire duration of that fetch. If bexio's auth host hangs (TCP accept but no response â€” a common failure mode for OAuth IdPs under incident), the fetch never resolves and the lock is held indefinitely.",
    "impact": "Every other token-refresh attempt (worker cron run AND the web app, which share the same lock key by design) blocks forever on the advisory lock. The hung transaction also pins one of only 5 pool connections; repeated stuck refreshers can exhaust the pool and freeze the whole web dashboard and the daily billing run â€” billing silently stops until the process is restarted. No watchdog kills it because neither the postgres-js pool nor the fetch sets a timeout.",
    "trigger": "bexio auth endpoint accepts the connection but stalls (incident, network black-hole, proxy hang) during a scheduled refresh window â€” i.e. exactly when the daily 08:00 cron fires and the access token is within the 60s refresh buffer.",
    "suggestedFix": "Wrap the token fetch in an AbortController with a hard timeout (e.g. AbortSignal.timeout(10_000)) in callTokenEndpoint, and ideally move the network fetch OUTSIDE the advisory-lock transaction: acquire lock â†’ re-read â†’ fetch with timeout â†’ write â†’ commit is fine, but the lock must not outlive a bounded call. At minimum add `statement_timeout`/`idle_in_transaction_session_timeout` to the pool and a fetch timeout so a hung auth host cannot pin the lock and a connection indefinitely. Apply the same timeout to callBexio's fetch (http.ts:77).",
    "confidence": 0.85,
    "verifyNote": "Confirmed in current code. auth.ts:63-108 opens a db.transaction, acquires pg_advisory_xact_lock(4242001) at line 64 (xact-scoped â†’ held until commit/rollback at line 108), then at line 93 awaits callTokenEndpoint, which in http.ts:100-107 does `await pace()` then a bare `fetch()` to https://auth.bexio.com/.../token with NO AbortSignal/timeout. Grep confirms zero AbortController/AbortSignal/signal: usages anywhere in the repo. Pool (db/index.ts:19-23) is max:5 with idle_timeout:20 and connect_timeout:10 only â€” no statement_timeout and no idle_in_transaction_session_timeout; connect_timeout bounds only connection establishment, not in-transaction lock waits. Worker runs on Bun, Web on @sveltejs/adapter-node (undici) â€” neither default-times-out a fetch where the auth host accepts the TCP connection but never responds. Both Worker (run.ts/cli.ts) and Web (api/sync, billing-runs, orders/import, subscriptions/[id]) call getValidAccessToken against the same DB and the SAME lock key 4242001, and pg_advisory_xact_lock is a blocking acquire, so a single stuck holder blocks every other refresher indefinitely and pins one of the 5 pool connections per stuck request. Trigger is external (bexio auth host accept-but-stall during a refresh window). Harm = silent billing halt + dashboard freeze requiring a process restart, with no watchdog â€” a stuck state that blocks future billing. Maps to P1 (not P0: it stalls billing rather than misbilling/leaking). No existing guard neutralizes it. Caveat lowering confidence: depends on Bun/undici truly never timing out an accepted-but-silent socket; in practice neither sets a default response timeout, so the unbounded-hang path is real.</reasoning>\n<parameter name=\"refinedFix\">Add a hard timeout to the network calls so a hung auth host cannot pin the lock or a pool connection. In http.ts, give both callTokenEndpoint (line 102) and callBexio (line 77) a `signal: AbortSignal.timeout(10_000)` (and catch the resulting TimeoutError/AbortError to classify as transient so the state machine retries). Best: also move the fetch outside the advisory-lock transaction â€” acquire lock â†’ re-read â†’ release/commit pattern isn't possible with xact lock across an await fetch without holding it, so instead acquire lock, re-read, do the (now-timeout-bounded) fetch, write, commit; the timeout guarantees the lock cannot outlive a bounded call. As defense-in-depth, add `statement_timeout` and `idle_in_transaction_session_timeout` to the postgres() options in db/index.ts:19-23 (e.g. via connection params) so any hung in-transaction wait is force-aborted server-side.</parameter>\n</invoke>\n",
    "refinedFix": "Wrap the token fetch in an AbortController with a hard timeout (e.g. AbortSignal.timeout(10_000)) in callTokenEndpoint, and ideally move the network fetch OUTSIDE the advisory-lock transaction: acquire lock â†’ re-read â†’ fetch with timeout â†’ write â†’ commit is fine, but the lock must not outlive a bounded call. At minimum add `statement_timeout`/`idle_in_transaction_session_timeout` to the pool and a fetch timeout so a hung auth host cannot pin the lock and a connection indefinitely. Apply the same timeout to callBexio's fetch (http.ts:77)."
  },
  {
    "title": "Token-endpoint errors are hard-coded to errorClass 'auth', masking transient 429/500 from the IdP",
    "severity": "P3",
    "category": "bug",
    "file": "packages/bexio-client/src/auth.ts",
    "lines": "94-97",
    "description": "When the token endpoint returns non-ok, the code throws `new BexioApiError(res.status, 'auth', body)` â€” the errorClass is hard-coded to 'auth' regardless of status. callTokenEndpoint (http.ts:100-107) returns the raw Response and never runs classifyStatus(), so a 429 (rate limit) or 5xx (transient outage) from auth.bexio.com is reported as class 'auth'. The 'auth' class is the project's signal for 'token invalid, run oauth-setup to re-auth' (mirrored in state-machine.ts:332-338).",
    "impact": "A transient bexio auth outage (500/503) or rate-limit (429) during the daily refresh is surfaced and logged/notified as 'auth â€” re-authenticate needed', sending Marcus down a manual re-auth path for what is actually a retryable outage. Conversely, retry/backoff logic keyed on errorClass would not treat a real 429/5xx as retryable. Pure observability/classification gap, but it delays correct detection on the single most critical credential path.",
    "trigger": "bexio's OAuth token endpoint returns 429 or 5xx during a scheduled refresh (IdP overload/incident).",
    "suggestedFix": "Run classifyStatus(res.status) for the token-endpoint error (or inspect the OAuth error body: only `invalid_grant`/`invalid_client` is truly 'auth'; 429â†’'rate_limit', 5xxâ†’'transient') so transient IdP failures are not mislabeled as a credential problem.",
    "confidence": 0.78,
    "verifyNote": "Code confirmed as described: auth.ts:94-97 throws BexioApiError(res.status, 'auth', ...) with errorClass hard-coded to 'auth'; callTokenEndpoint (http.ts:100-107) returns the raw Response and never runs classifyStatus(). So a 429/5xx from auth.bexio.com during the daily refresh is mislabeled 'auth' (the project's 're-authenticate' signal, mirrored at state-machine.ts:332-338). The path is reachable: getValidAccessToken is called once at run.ts:92, before the order loop, and a throw there aborts the whole run with an 'auth'-labeled failure. The mislabeling half of the finding is REAL.\\n\\nThe finding's second, more serious claim â€” 'retry/backoff logic keyed on errorClass would not treat a real 429/5xx as retryable' â€” is REFUTED. The only errorClass-keyed retry classifier is classifyForRetry (subscriptions.ts:97-104), and the subscription pipeline is fully retired dead code (run.ts:131-138: no new subscriptions, worker no longer bills/reconciles; subscriptionResults hard-coded empty). The order pipeline (state-machine.ts:332-344) branches on err.status===401/403 and errorClass==='permanent', none of which a token 429/5xx reaches â€” and it can't anyway, since the token error throws earlier at run.ts:92 outside the per-order try/catch. There is no retryable-vs-fatal decision keyed on the token endpoint's errorClass. Net harm is purely a misleading log/notification string on the credential path, not wrong retry behavior or any money/billing impact. P3 is correct (arguably generous).",
    "refinedFix": "In auth.ts:94-97, classify the token-endpoint error instead of hard-coding 'auth'. Prefer inspecting the OAuth error body: only invalid_grant/invalid_client is truly 'auth'; otherwise map 429 -> 'rate_limit' and 5xx -> 'transient' (export and reuse classifyStatus from http.ts as the fallback). This makes a transient IdP outage surface as transient/rate_limit in the run-failure notification rather than sending Marcus down a manual oauth-setup re-auth path. Note: this is purely a labeling/observability fix â€” no current retry control flow keys off this errorClass (the only such consumer, classifyForRetry in the subscription pipeline, is dead code)."
  },
  {
    "title": "Snapshot/order invoice positions silently emit CHF 0 lines (no zero-amount/zero-price guard)",
    "severity": "P2",
    "category": "edge-case",
    "file": "packages/bexio-client/src/invoices.ts",
    "lines": "107-144",
    "description": "mapOrderPositionToInvoicePosition() coerces missing money fields to zero with no validation: KbPositionCustom unit_price falls back to '0' (line 140), amount falls back via `position.amount ?? '1'` but a literal numeric 0 amount becomes '0' (line 114), and KbPositionArticle omits unit_price when null so bexio uses the article price. buildCreateInvoiceInputFromOrder() only throws when positions.length === 0 (line 87-89) â€” it never checks that the resulting invoice total is non-zero. Unlike the subscription path, which has validateSubscriptionInputs() rejecting zero sale_price, the order/snapshot path (daily/weekly recurring) has NO zero-amount guard anywhere in the client.",
    "impact": "A recurring order whose position has a null/zero unit_price (e.g. a placeholder line, a mis-edited order, or a position type that bexio stores price-less) produces a CHF 0.00 invoice that is auto-issued (festgeschrieben) and emailed to the customer. A festgeschrieben CHF 0 invoice cannot be deleted, only credited â€” it is a visible billing error sent to a real customer.",
    "trigger": "A daily/weekly recurring order reaches the snapshot fallback path with at least one custom position that has unit_price=null (or amount 0). buildCreateInvoiceInputFromOrder builds a position with unit_price '0' and the pipeline issues + sends it.",
    "suggestedFix": "After building positions in buildCreateInvoiceInputFromOrder, sum the line totals (amount Ã— unit_price, minus discount) and throw if the computed total is <= 0, mirroring validateSubscriptionInputs. At minimum, throw when every position resolves to unit_price 0/null.",
    "confidence": 0.62,
    "verifyNote": "Code claims verified exactly. packages/bexio-client/src/invoices.ts:140 coerces a custom position's unit_price to '0' when null; :122 omits unit_price for article positions (bexio then uses catalog price); buildCreateInvoiceInputFromOrder (:87-93) only guards positions.length===0 and missing user_id â€” no total/price check. The asymmetry the finding relies on is real: subscriptions.ts validateSubscriptionInputs (:76-83) explicitly throws on null/zero sale_price, while the order/snapshot path has no equivalent anywhere. Reachability confirmed: state-machine.ts only reaches createInvoiceFromOrderSnapshot for daily/weekly orders (shouldSnapshotFallback, :103-105/:259) after a 422 fully-invoiced, then unconditionally issues (:400) and sends (:425) using only invoice.id; it never inspects invoice.total against zero, so there is no downstream guard either. The harmful end state (a festgeschrieben CHF 0 invoice emailed to a real customer) is real and customer-visible. HOWEVER it requires a misconfigured order â€” a custom position with unit_price=null/0 (placeholder/heading/mis-edited line) such that the WHOLE invoice totals 0; a single zero line mixed with priced lines is harmless (total stays positive), and the article path falls back to catalog price so it rarely zeroes out. That makes it a narrow, operator-error-triggered edge case, not normal-data behavior. It is incorrect-but-recoverable (credit note fixes it), does not lose money or double-bill, and does not block future billing â€” so P2 (missing-validation/edge-case), not P1. Not a duplicate of any KNOWN-ALREADY-FIXED item (N-7/N-8 covered the subscription path only).",
    "refinedFix": "The finder's \"sum amount Ã— unit_price âˆ’ discount in the client\" is fragile: the client cannot know an article's catalog price (unit_price is omitted, not 0), and discount/mwst semantics complicate a client-side total. Prefer two layers: (1) In buildCreateInvoiceInputFromOrder, throw only for the fully-client-decidable case â€” every resolved KbPositionCustom position has unit_price null/0 AND there are no KbPositionArticle positions (so the total is provably 0). (2) Authoritative guard in state-machine.ts processOrder AFTER creation: bexio already returns invoice.total (used at lines 290/389/437); before transitioning to 'issuing', if Number(invoice.total) <= 0, deleteClaim + return {kind:'failed', reason:'refusing to issue CHF 0 invoice'} (or 'not_due'). This catches both the custom-null and the article-fallback-zero cases regardless of source, and runs before any festschreiben/send side effect. Apply the same post-create total<=0 guard to the subscription path for symmetry."
  },
  {
    "title": "Rate-limit retry-after is parsed but never propagated to pacing; 429s are treated as plain transient failures",
    "severity": "P3",
    "category": "bug",
    "file": "packages/bexio-client/src/http.ts",
    "lines": "82-90",
    "description": "callBexio parses the Retry-After header into BexioApiError.retryAfterSeconds for rate_limit (429) errors, but nothing in the codebase ever reads retryAfterSeconds (confirmed: only set in http.ts, declared in types.ts; zero consumers). The pace() function uses a fixed 1100ms gap and is not informed by a 429. Callers (state-machine processOrder generic-failure branch, subscriptions classifyForRetry) treat 429 like any transient failure and just retry on the NEXT daily run, with no honoring of the server's backoff hint within a run.",
    "impact": "Under a burst (catch-up loop billing many subscriptions/orders, or Promise.all article fetches) that trips bexio's 60/min limit, the run does not back off as instructed; affected orders fail for the day and wait ~24h for the next cron. Delayed billing, not wrong billing. The retryAfterSeconds field is effectively dead code, giving a false impression that backoff is handled.",
    "trigger": "bexio returns 429 with Retry-After during a run with many sequential API calls. The hint is captured and discarded.",
    "suggestedFix": "Either consume retryAfterSeconds in pace()/a retry wrapper inside callBexio (sleep the hinted duration and retry the single call once), or remove the dead field and document that 429 handling is next-run only.",
    "confidence": 0.9,
    "verifyNote": "Confirmed in current code. http.ts:82-90 parses the retry-after header for 429 (rate_limit) errors and stores it as BexioApiError.retryAfterSeconds (declared types.ts:305). Grep across the whole repo shows zero readers of retryAfterSeconds â€” it is set in exactly one place and read nowhere, so it is dead code. The pacer pace() uses a fixed MIN_GAP_MS=1100 and is never informed by a 429. Both callers ignore the hint: classifyForRetry (subscriptions.ts:97-104) lumps rate_limit with transient -> delete-and-retry, which only deletes the billing_runs lock so the NEXT daily cron retries; state-machine.ts marks the row failed/issued with no backoff branch. The only setTimeout in worker code is the fixed gap in pace() and oauth-setup â€” no in-run sleep honors the server's backoff hint. So the description is accurate and the path is reachable (a 429 mid-run is not backed off within the run; the order/subscription waits ~24h). Severity P3 is correct: the harm is DELAYED billing, not wrong/double billing â€” no money-correctness impact. Reachability is also low in this production profile because pace() already serializes ALL calls (including the single Promise.all at subscriptions.ts:193) into one ~54 req/min queue, below bexio's 60/min, so a 429 burst requires a large catch-up loop or external concurrent runs rather than normal daily operation. The field is genuinely misleading dead code, justifying a low-priority cleanup.",
    "refinedFix": "Pick one of two: (a) Honor the hint inside callBexio: on a 429 with retryAfterSeconds present, sleep min(retryAfterSeconds*1000, a sane cap e.g. 30s) and retry the single call once before throwing â€” keep the existing classify/throw as the fallback so callers still see rate_limit if the retry also fails. (b) If next-run-only handling is intentional, delete the retryAfterSeconds field from BexioApiError (types.ts:305) and the parsing block at http.ts:82-89, and add a comment that 429 is handled as delete-and-retry on the next cron. Option (a) is preferable since it reduces ~24h billing delays under bursts at near-zero risk. Note: any in-call retry must NOT re-issue/re-send money-moving POSTs blindly â€” confine the retry to the failed call only; since callBexio is a single request and idempotency is enforced at the state-machine/lock layer, a single bounded retry of the same request is safe."
  },
  {
    "title": "Pagination uses an absolute 5000-offset cap and full-page client-side filtering, so recurring orders beyond #5000 are silently dropped",
    "severity": "P3",
    "category": "edge-case",
    "file": "packages/bexio-client/src/orders.ts",
    "lines": "12-38",
    "description": "listRecurringOrders pages /kb_order with limit+offset and filters is_recurring client-side, but caps at offset > 5000 with a warn+break (not a throw). Because bexio has no server-side is_recurring filter, the 5000 cap is against the TOTAL order count, not the recurring subset. An account with >5000 total kb_orders (recurring + one-off) will stop paging before reaching recurring orders that sort after position 5000, and they are silently excluded from billing. listRecurringOrdersSince has the same cap. Contrast listContacts/listArticles which THROW at 5000 rather than silently truncating.",
    "impact": "A recurring order beyond the 5000th total order is never synced, so it is never billed â€” silent revenue loss with only a console.warn (no notification, no error row). For a small business this is unlikely now but is an unbounded silent-failure mode as order history grows over years.",
    "trigger": "Total kb_order count in bexio exceeds 5000 (years of one-off invoices/orders accumulate) and a recurring order sorts after offset 5000.",
    "suggestedFix": "Sort/scope the listing (e.g. by status or via /kb_order/search on a recurring-correlated field) so the recurring subset is reachable, raise the cap, or at least surface the truncation as a run error/notification instead of a console.warn so it is detectable.",
    "confidence": 0.78,
    "verifyNote": "Confirmed against current code. packages/bexio-client/src/orders.ts lines 12-38 (listRecurringOrders) and 95-119 (listRecurringOrdersSince) both filter is_recurring CLIENT-SIDE (lines 23, 109) after a server-side page fetch, then break at offset > 5000 with only console.warn (lines 31-34, 112-115) â€” no throw, no run-error, no Discord notification. Because bexio has no server-side is_recurring filter (the code comment at lines 64-65/74-75 explicitly states is_recurring returns 400 on /kb_order/search), the 5000 cap is measured against TOTAL kb_order count, not the recurring subset. So a recurring order that sorts after total position 5000 is silently excluded from the sync, never tracked, and never billed. Reachability is genuine: both functions are on the production sync path (apps/worker/src/lib/sync.ts lines 168 and 170 â€” incremental and full modes respectively). The contrast the finding cites is also real: contacts.ts line 29 throws ('refusing to page further') at the same >5000 boundary, so the recurring-order lister is the inconsistent silent-truncation case. NOT refuted by any guard I could find. HOWEVER the trigger threshold is very high: this deployment is a small business (docs note <50 contacts, a daily canary, a handful of recurring orders), so accumulating >5000 total kb_orders would take many years, and the harm is bounded to orders beyond #5000. There is also a memory note (id 35594, 2026-06-01) claiming /kb_order/search DOES support an is_recurring filter, which if true would make a clean fix trivial â€” but the current code is deliberately written around the opposite assumption, so I treat the silent-truncation as live. This is correctly defense-in-depth / unbounded-future-risk, not an imminent money loss. P3 is the right severity; the worthwhile fix is to surface truncation as a run error/notification (like contacts throws) rather than a swallowed console.warn, so an actual hit is detectable in a money-moving pipeline.",
    "refinedFix": "Do not silently break. At minimum, when offset > 5000 is hit, record a run-level error and emit a Discord notification (the pipeline already has packages/notify) so the truncation is detectable rather than swallowed in a console.warn â€” symmetry with listContacts/listArticles which throw. Better: verify the memory-noted claim that POST /kb_order/search accepts an is_recurring criterion; if it does, replace the client-side filter + total-count paging with a server-side is_recurring=true search so the cap applies to the recurring subset (which is tiny) instead of total order history. If is_recurring truly is not searchable, raise the cap substantially and/or page by id ascending while tracking the max id seen, and only stop when a genuinely empty page returns â€” never on an arbitrary offset against the unfiltered total."
  },
  {
    "title": "No origin-side auth on dashboard page routes + form actions â€” CF Access is the ONLY gate, and the origin is admittedly reachable directly",
    "severity": "P1",
    "category": "security",
    "file": "apps/web/src/hooks.server.ts",
    "lines": "1-22",
    "description": "hooks.server.ts exports ONLY handleError â€” there is no `handle` hook (confirmed: it is the sole hooks file, and grep finds no `export const handle`). Origin-side CF Access verification (verifyCfAccess) is wired into exactly three routes: /api/trigger-run, /api/sync, /api/runs/[id]. Every page route and its form actions perform real reads/mutations with NO origin-side verification: `/` toggle action flips recurringOrders.enabled (+page.server.ts:50-67), `/settings` actions rewrite invoice_mail_subject/message, order_due_window_days, auto_send_invoices and the Discord webhook URL (settings/+page.server.ts:92-164), `/orders/import` runs syncNow/importOne hitting bexio (orders/import/+page.server.ts:36-70), and all load() functions dump every customer/order/run row to the response. These rely solely on Cloudflare Access at the edge. cf-access.ts:9-11 and DEPLOYMENT.md:214-220 both explicitly state the Hetzner origin-lock firewall is a not-yet-done 'follow-up' and that 'anyone who learns the origin IP can bypass CF Access by hitting it directly.' The /api/* endpoints got origin-side checks precisely for this reason; the equally-powerful page actions did not.",
    "impact": "An attacker who discovers the Coolify origin IP (Shodan, cert transparency, DNS history, subdomain leak) can hit https://<origin-ip>/ directly with no Cloudflare Access cookie and: read all customer names/orders/run history, toggle any recurring order's billing on or off, rewrite the invoice email subject/body sent to real customers, flip auto_send_invoices and order_due_window_days (which govern whether the next worker run auto-sends real invoices and to whom), and overwrite the Discord notification webhook to silence alerts. These settings directly steer money-moving behavior of the daily run, so this is one IP-discovery away from billing manipulation.",
    "trigger": "Origin IP becomes known (it is internet-reachable on 80/443 â€” origin-lock firewall not configured) and attacker sends plain HTTP requests / form POSTs to page routes, omitting the CF Access JWT entirely.",
    "suggestedFix": "Add a global `handle` hook in hooks.server.ts that calls verifyCfAccess(event.request) for every route except /health (and the bexio /callback, which carries the user's CF cookie from the browser redirect), returning 401 on CfAccessError. This makes the origin fail-closed regardless of the firewall, matching the protection already on /api/*. Independently, complete the Hetzner/ufw origin-lock from DEPLOYMENT.md:216-234 as defense-in-depth.",
    "confidence": 0.9,
    "verifyNote": "Confirmed by reading the actual files. hooks.server.ts (E:\\Git\\bexio bot\\apps\\web\\src\\hooks.server.ts) exports ONLY handleError â€” no `handle` hook, and grep across apps/web/src finds no `export const handle`; it is the sole hooks file. verifyCfAccess is wired into exactly three routes (api/trigger-run/+server.ts:37,119-121, api/sync, api/runs/[id]) and nowhere else. The page form actions perform real, unauthenticated-at-origin mutations: +page.server.ts:50-67 `toggle` flips recurringOrders.enabled (governs whether an order is billed); settings/+page.server.ts:92-164 rewrites invoice_mail_subject/message, order_due_window_days, auto_send_invoices, and the Discord webhook URL; orders/import/+page.server.ts:36-70 calls getValidAccessToken + syncRecurringOrders/importOrderById against bexio. All load() functions dump full customer/order/run rows. Reachability is real: DEPLOYMENT.md:216-220 and cf-access.ts:8-11 both explicitly state the Hetzner/ufw origin-lock is a not-yet-done follow-up and that anyone who learns the origin IP can bypass CF Access by hitting 80/443 directly. No neutralizing guard exists â€” there is no middleware or per-load auth helper; the /api/* endpoints got origin-side checks for exactly this reason while the equally-powerful page actions did not. These settings (auto_send_invoices, order_due_window_days, order enable, invoice email body) directly steer the daily money-moving run, so this is one IP-discovery step from billing manipulation. Severity P1 is correct per the project rubric (exploitable-but-gated security issue): gated behind origin-IP discovery and the actual invoice send still requires a cron/CF-gated worker run, so it is not a direct single-request P0 money loss, but it is clearly more than recoverable misbehavior.",
    "refinedFix": "Add a global `handle` hook in apps/web/src/hooks.server.ts that calls verifyCfAccess(event.request) for every request and returns 401 on CfAccessError (500 on server-misconfigured), making the origin fail-closed like /api/* already is. Allowlist ONLY a genuinely public /health endpoint (the Coolify canary). Do NOT exempt the bexio /callback route: it is hit by the user's browser carrying their CF Access cookie and is itself behind the CF Access app, so exempting it would open an unauthenticated OAuth-callback surface â€” let it flow through verifyCfAccess like every other page. Independently complete the Hetzner/ufw origin-lock from DEPLOYMENT.md:216-234 as defense-in-depth."
  },
  {
    "title": "/health leaks internal DB error strings unauthenticated",
    "severity": "P3",
    "category": "security",
    "file": "apps/web/src/routes/health/+server.ts",
    "lines": "38-43",
    "description": "The /health endpoint is intentionally unauthenticated (Coolify canary) and, on a DB failure, returns the raw exception message in the JSON body: `error: err instanceof Error ? err.message : String(err)`. Postgres/driver errors can include connection-string fragments, host/port, role names, or schema details. Because there is no `handle` hook, this route is reachable both via CF and directly on the origin IP.",
    "impact": "Low-value reconnaissance: an unauthenticated caller hitting /health during a DB outage learns internal infrastructure details (db host, role, driver internals) that aid further targeting. No direct money/data loss.",
    "trigger": "Postgres is unreachable or misconfigured and any party (including a direct-to-origin caller) requests /health, receiving the verbatim driver error.",
    "suggestedFix": "Return a generic `{ status: 'db_error' }` with HTTP 503 and log the detailed message server-side only (console.error), rather than echoing err.message in the response body.",
    "confidence": 0.82,
    "verifyNote": "Confirmed in current code. apps/web/src/routes/health/+server.ts lines 38-43 return the raw driver error in the JSON body on a DB exception: `error: err instanceof Error ? err.message : String(err)` with HTTP 503. The route is unauthenticated: it calls no auth helper, and hooks.server.ts exports ONLY `handleError` (no `handle` hook) â€” verified via grep `export const handle\\b` = no matches â€” so there is no centralized auth boundary; auth is per-endpoint via verifyCfAccess, which /health does not call. The catch block returns a normal Response, so it bypasses handleError entirely (handleError only fires on thrown errors and only logs server-side; it does not sanitize this body). Direct-to-origin reachability is real: cf-access.ts's own comment states origin-direct hits are possible and the Hetzner firewall IP-lock is future work (\"Once the Hetzner firewall locks origin to CF IPs only, this becomes belt-and-suspenders\"). The DB layer uses postgres.js (packages/db/src/index.ts); its connection-failure messages contain host/port, role name, and database name (e.g. `connect ECONNREFUSED host:5432`, `password authentication failed for user \"bexiobot\"`), which is genuine low-value recon. Crucially it does NOT echo the connection string/password, so this is info-disclosure, not a secret leak â€” no money/data loss, and it only triggers while Postgres is actually down/misconfigured. P3 is correct.",
    "refinedFix": "In the catch block (lines 38-43), log the detail server-side only and return a generic body: `console.error('[health] db_error:', err);` then `return new Response(JSON.stringify({ status: 'db_error' }), { status: 503, headers: { 'Content-Type': 'application/json' } });`. Do not include err.message in the response body. (Optional defense-in-depth: also add the planned Hetzner firewall IP-allowlist so the origin is not directly reachable, which closes the direct-to-origin vector for all unauthenticated routes.)"
  },
  {
    "title": "Page server actions have no origin-side CF Access check â€” direct-origin hit bypasses auth and can send real invoices / reset billing",
    "severity": "P1",
    "category": "security",
    "file": "apps/web/src/routes/subscriptions/[id]/+page.server.ts",
    "lines": "58-102",
    "description": "Only the three JSON API endpoints (api/trigger-run, api/sync, api/runs/[id]) call verifyCfAccess(). The SvelteKit form-action handlers do not, and there is NO global edge guard: hooks.server.ts (apps/web/src/hooks.server.ts) exports only handleError, there is no `handle` hook, and there is no +layout.server.ts performing auth (confirmed: grep for `export const handle` matches only handleError; no +layout.server.ts exists). Auth for page routes therefore relies ENTIRELY on Cloudflare Access at the edge. The cf-access.ts header (lines 8-11) and DEPLOYMENT.md lines 216-220 both state the origin is reachable directly until the Hetzner Origin-Lock is applied â€” and that lock is still an open TODO (memory feedback_proactive_lago_reminder.md item 2; DEPLOYMENT.md lists it under 'Defense-in-depth follow-ups', not done). The `runNow` action (lines 94-101) calls runSubscriptionNow -> processOneSubscription, which CREATES AND SENDS a real invoice. `resume` (lines 70-81) sets status='active' and nextBillingDate=today so the next cron bills immediately; `pause`/`cancel` change billing state.",
    "impact": "An unauthenticated actor who learns the origin IP/hostname (cert-transparency logs, historical DNS, Shodan, Coolify default host) can POST directly to /subscriptions/<id>?/runNow and cause the bot to generate and email a real invoice to a real customer, or use ?/resume to force a subscription back into the billing cycle â€” all with zero authentication. Money/wrong-billing impact, plus billing-state tampering.",
    "trigger": "Origin IP discovered (it is not behind the not-yet-applied firewall lock) + a POST with a form-action body (e.g. `?/runNow`) to https://<origin>/subscriptions/<existing-id>. No CF JWT, no session, no CSRF token required because the request is same-origin/direct (CSRF origin-check only stops cross-site browser POSTs, not a direct scripted request).",
    "suggestedFix": "Add a SvelteKit `handle` hook in hooks.server.ts that calls verifyCfAccess(event.request) for every non-public route (or a shared +layout.server.ts guard), so origin-side verification protects page actions exactly like the API endpoints â€” do not depend solely on the edge or the still-pending firewall lock. At minimum, gate the money-moving runNow/resume actions behind verifyCfAccess.",
    "confidence": 0.85,
    "verifyNote": "Confirmed at code level and reachable. apps/web/src/routes/subscriptions/[id]/+page.server.ts lines 58-102: the four actions (pause/resume/cancel/runNow) perform zero auth â€” no verifyCfAccess call. Grep confirms verifyCfAccess is invoked ONLY in the three /api/* JSON endpoints (api/sync line 18, api/runs/[id] line 19, api/trigger-run line 37), never in page actions or load. hooks.server.ts exports only handleError (no `handle` auth hook), and no +layout.server.ts exists anywhere â€” so page-route auth depends ENTIRELY on the CF Access edge. runNow (lines 94-101) calls runSubscriptionNow -> processOneSubscription, which creates and SENDS a real invoice (corroborated by probe-subscription.ts header: \"this creates a real bexio invoice draft\"). resume (lines 70-81) sets status='active' and nextBillingDate=today so the next cron bills immediately. DEPLOYMENT.md lines 214-220 explicitly list the Cloudflare Origin-Lock as a not-yet-done \"Defense-in-depth follow-up\" and state verbatim \"Without this lock, anyone who learns the origin IP can bypass CF Access by hitting it directly.\" cf-access.ts lines 8-11 confirm origin-side verification was the intended defense-in-depth, but it was wired into the API endpoints only, not the page actions. On the CSRF question: svelte.config.js uses adapter-node with default CSRF, but SvelteKit's checkOrigin only blocks cross-site browser POSTs via Origin-header comparison; a direct scripted request to the discovered origin (curl/fetch with a matching or omitted Origin, or a non-form content-type) is not stopped â€” exactly the trigger described. The harm (generate+email a real invoice, or force a subscription back into billing) is therefore reachable by any actor who discovers the origin IP/hostname, with no credential required. The developer added verifyCfAccess to all three API endpoints precisely because the edge can be bypassed, but omitted the page actions â€” the most dangerous surface (runNow moves money). The only thing keeping severity at P1 rather than P0 is that exploitation is gated behind origin-IP discovery (recon) rather than being directly internet-reachable by hostname.",
    "refinedFix": "Add a SvelteKit `handle` hook in apps/web/src/hooks.server.ts (alongside handleError) that calls verifyCfAccess(event.request) for every non-public route (allow-list /health and any unauthenticated bexio OAuth callback paths), returning 401 on CfAccessError. This protects page loads AND form actions identically to the API endpoints and removes the sole reliance on the still-pending Hetzner origin firewall lock. At minimum, immediately gate the money-moving runNow and the billing-state-mutating resume/pause/cancel actions (and the data-leaking load) behind verifyCfAccess. Also apply the firewall origin-lock from DEPLOYMENT.md lines 222-235 as the second layer."
  },
  {
    "title": "Settings and order-import form actions are state-changing yet unauthenticated at the origin",
    "severity": "P2",
    "category": "security",
    "file": "apps/web/src/routes/settings/+page.server.ts",
    "lines": "92-164",
    "description": "Same root cause as the subscriptions finding: the settings actions (notifications/invoicing/advanced) and orders/import actions (syncNow/importOne, apps/web/src/routes/orders/import/+page.server.ts lines 36-70) perform no verifyCfAccess and there is no global handle/layout guard. These actions write app_settings that the worker reads at runtime: invoicing (lines 119-146) flips auto_send_invoices and rewrites the invoice mail subject/body that gets emailed to customers and sets order_due_window_days (the due-gate); advanced (149-163) toggles incremental_sync_enabled and run_stale_minutes; notifications (94-116) overwrites discord_webhook_url. importOne triggers bexio API calls (token spend / arbitrary order import).",
    "impact": "A direct-origin (CF-bypassing) request can silently change billing behavior: turn auto-send on/off, alter the customer-facing email body, widen the due window so every order bills, redirect the Discord webhook to exfiltrate run notifications, or disable incremental sync. Recoverable but a real tampering / observability-loss vector on a money-moving system.",
    "trigger": "Direct POST to /settings?/invoicing (or /orders/import?/importOne) at the origin IP while the firewall origin-lock is not yet in place.",
    "suggestedFix": "Cover these routes with the same `handle`-hook verifyCfAccess guard recommended above so every state-changing action is authenticated origin-side, not just the /api/* endpoints.",
    "confidence": 0.85,
    "verifyNote": "Confirmed accurate against current code. The five form actions â€” settings notifications (L94), invoicing (L119), advanced (L149) and orders/import syncNow (L37), importOne (L48) â€” perform NO auth: they call setSetting()/getValidAccessToken() directly with no verifyCfAccess. Critically, the finding's core premise holds: there is NO global handle hook. hooks.server.ts (E:/Git/bexio bot/apps/web/src/hooks.server.ts) exports ONLY handleError (an error logger), not handle. Glob found no +layout.server.ts or +layout.ts anywhere in the route tree. Grep for 'export const handle' returns zero matches. The ONLY origin-side auth in the whole web app is the three explicit verifyCfAccess() calls in /api/trigger-run, /api/sync, /api/runs/[id]. So the page form actions are protected solely by Cloudflare Access at the edge, with none of the defense-in-depth the /api/* endpoints deliberately added. cf-access.ts L8-11 documents the intent: origin-side verification exists precisely so a CF-bypassing direct-origin hit is rejected, and notes the Hetzner firewall origin-lock is a future/conditional state. A direct POST to /settings?/invoicing reaching the origin executes with no JWT check: it can flip auto_send_invoices, rewrite the customer-facing invoice mail body, widen order_due_window_days (the billing due-gate), or redirect discord_webhook_url (exfiltrating run-notification embeds â€” though the webhook is masked on READ, write is unguarded). importOne spends bexio tokens / imports arbitrary orders. Real tampering/observability vector on a money-moving system. Not P0 (no direct money move on call, no secret leak â€” DB secrets never editable, webhook masked on read; worker only acts on next cron with the tampered settings) and reachability is edge-gated, so P2 is correct.",
    "refinedFix": "Add a single export const handle in apps/web/src/hooks.server.ts that calls verifyCfAccess(event.request) for every non-public route (allow only /health and the bexio OAuth callback/, which CF Access already fronts but must remain reachable for the OAuth redirect), returning 401 on CfAccessError. This closes the same gap for ALL page form actions at once and matches the defense-in-depth already applied to /api/*, rather than sprinkling per-action checks. Verify the OAuth callback and /health route exemptions so the daily worker trigger and bootstrap canary still function."
  },
  {
    "title": "discord_webhook_url accepts any https:// host â€” server-side request to internal/arbitrary host (SSRF) when worker posts notifications",
    "severity": "P3",
    "category": "security",
    "file": "apps/web/src/routes/settings/+page.server.ts",
    "lines": "107-113",
    "description": "The notifications action validates discord_webhook_url with only `/^https:\\/\\/.+/` (line 109). It does not constrain the host to discord.com / discordapp.com. The worker (packages/notify) later POSTs invoice-run summaries to whatever URL is stored. There is no allowlist, so the URL can point at an internal address (e.g. https://169.254.169.254/..., https://localhost:.../, an internal Coolify service) or any attacker endpoint.",
    "impact": "If an attacker reaches this action (see the auth findings above) they can turn the worker into an SSRF client and exfiltrate run notifications (which include invoice/customer counts) to an arbitrary host. Even for the legitimate admin it is a foot-gun. Low severity because it is gated behind the (broken) auth and the body is limited to notification embeds.",
    "trigger": "Setting discord_webhook_url to a non-Discord https URL; the next worker run / notification POSTs run data to that host.",
    "suggestedFix": "Restrict the webhook host to discord.com / discordapp.com (or an explicit allowlist) and reject private/loopback/link-local IP literals.",
    "confidence": 0.78,
    "verifyNote": "Code-grounded and reachable. apps/web/src/routes/settings/+page.server.ts:107-113 validates discord_webhook_url with only /^https:\\/\\/.+/ â€” no host allowlist. The stored value flows to the worker: loadWorkerSettings (apps/worker/src/lib/settings.ts:83) reads discord_webhook_url from app_settings, and packages/notify/src/discord.ts:86-92 (postWebhook) does fetch(webhookUrl, {method:'POST',...}) with zero host validation. So setting the field to https://169.254.169.254/... or any internal/arbitrary host makes the worker POST run-report data there on the next run. The SSRF sink is real.\n\nSeverity is correctly P3, not higher, for three reasons: (1) The action has NO origin-side auth â€” hooks.server.ts exports only handleError, not handle; verifyCfAccess is wired only into /api/trigger-run, /api/sync, /api/runs/[id], NOT the /settings page actions â€” so the only barrier is Cloudflare Access at the edge. The attacker must already be the authenticated admin (then it is a foot-gun, not an exploit) or have separately bypassed CF Access. (2) The exfiltrated body is limited to notification embeds (invoice/customer counts, names, CHF amounts) â€” no secrets/OAuth tokens are ever read into the embed (the secrets table is untouched here). (3) It is a BLIND SSRF POST: postWebhook discards the response (only returns ok/status/truncated-text on failure, not surfaced to any attacker), so the cloud-metadata read-back angle does not yield response exfiltration. Net: a legitimate defense-in-depth gap, low value, auth-gated â€” exactly P3 as the finder claimed.",
    "refinedFix": "Restrict the webhook host in the notifications action to an explicit allowlist (discord.com, discordapp.com, canary.discord.com, ptb.discord.com) AND reject private/loopback/link-local IP literals. Parse with new URL(webhook), require url.protocol==='https:', require the hostname to end with one of the allowed Discord domains, and reject if the hostname is an IP in 127.0.0.0/8, 10/8, 172.16/12, 192.168/16, 169.254/16, ::1, or fc00::/7. Apply the same allowlist check in loadWorkerSettings / before postWebhook as belt-and-suspenders, since the DB value could also be set out-of-band. Keep severity P3."
  },
  {
    "title": "order_due_window_days has no upper bound â€” a large value makes every order perpetually 'due'",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/web/src/routes/settings/+page.server.ts",
    "lines": "125-142",
    "description": "The invoicing action validates order_due_window_days only as an integer >= 0 (line 136). There is no maximum. The worker's order due-gate (isOrderDue + ORDER_DUE_WINDOW_DAYS, per project memory project_order_due_gate) treats any order whose next-billing falls within the window as due. A value like 99999 effectively disables the due-gate, so monthly/yearly orders would bill far ahead of schedule â€” re-introducing the exact 'fires immediately' bug the gate was added to fix.",
    "impact": "Mis-set (or maliciously set, via the auth gap above) huge due window causes premature billing of future-dated recurring orders. Recoverable but wrong-timing invoices = early charges to customers.",
    "trigger": "Admin (or attacker via the unauthenticated settings action) saves order_due_window_days as a very large integer; next daily run bills orders that are not yet due.",
    "suggestedFix": "Add a sane upper bound (e.g. due <= 31, or <= the smallest billing period in days) and reject values above it in the invoicing action.",
    "confidence": 0.6,
    "verifyNote": "CONFIRMED that the validation has no upper bound: apps/web/src/routes/settings/+page.server.ts:136 only rejects non-integers and negatives (`!Number.isInteger(due) || due < 0`). The worker's parseWindowDays (apps/worker/src/lib/settings.ts:58-61) likewise accepts any finite n>=0. So a value like 99999 is stored and used as the due window.\\n\\nBUT the finder's described MECHANISM and IMPACT are WRONG, which lowers the real severity. The due-gate (isOrderDue, apps/worker/src/lib/next-billing.ts:273-289) is NOT a look-ahead window. computeCurrentOccurrence (lines 204-256) caps its horizon at `today` (line 218) and only ever returns the latest occurrence ON OR BEFORE now â€” it never considers future occurrences. isOrderDue then computes diffDays = today - occurrence (always >= 0, line 285) and returns due iff diffDays <= windowDays. Therefore a huge window does NOT 'bill orders ahead of schedule' or 'bill future-dated recurring orders' as the finding claims â€” future occurrences are never pulled forward. What a huge window actually does is remove the back-catch-up bound: a newly-onboarded order with an old start date (whose last occurrence is weeks/months in the past) would be billed immediately for that single most-recent past period, instead of waiting for its next occurrence. This re-weakens the protection the gate added â€” but it bills AT MOST ONE occurrence (computeCurrentOccurrence returns only the single latest, never a backlog), and the (order_id, billing_period) dedup prevents double-billing.\\n\\nReachability: requires an admin to deliberately type an absurd value into the /settings invoicing form. The 'attacker via the unauthenticated action' angle the finding leans on is a SEPARATE issue (no SvelteKit handle hook, per observation 39424), and even that is fronted by Cloudflare Access over the whole app. The single call site is state-machine.ts:184; computeNextBilling is correctly NOT used as a gate (matches project_order_due_gate memory).\\n\\nNet: the missing upper bound is genuinely absent and a huge value measurably weakens the due-gate (defense-in-depth gap / missing validation), so isReal=true â€” but it is wrong-timing-by-one-period at worst, dedup-protected, single-occurrence, and only via deliberate misconfiguration. P3 is correct; it is not a money-loss P0/P1. The finder's P3 stands, but its impact narrative should be corrected.",
    "refinedFix": "Add a sane upper bound on order_due_window_days in the invoicing action (apps/web/src/routes/settings/+page.server.ts:136), e.g. reject due > 31 with a clear message, since a catch-up tolerance beyond ~a month has no legitimate use and only serves to back-bill a stale onboarded order's latest past period. Mirror the clamp in parseWindowDays (apps/worker/src/lib/settings.ts:58-61) so an out-of-range DB/env value is also bounded server-side (e.g. `return Number.isFinite(n) && n >= 0 ? Math.min(n, 31) : 3`). Note in the fix that the real risk is back-billing the most-recent past occurrence of a freshly-activated order, NOT premature billing of future-dated orders â€” future occurrences are never pulled forward by the gate."
  },
  {
    "title": "SvelteKit page form actions (incl. money-moving retry) have no origin-side CF Access verification; relies solely on the edge",
    "severity": "P1",
    "category": "security",
    "file": "apps/web/src/hooks.server.ts",
    "lines": "1-21",
    "description": "hooks.server.ts exports only handleError â€” there is no `handle` hook, and App.Locals is empty (app.d.ts L7). The JSON endpoints (/api/trigger-run, /api/sync, /api/runs/[id]) each call verifyCfAccess() in code, but the SvelteKit *page* routes and their form actions do NOT. billing-runs/+page.server.ts `retry` action (L29-42) calls runSubscriptionNow() which creates AND sends a real invoice to a real customer; +page.server.ts `toggle` action (L50-67) flips recurring_orders.enabled, which gates whether the bot bills an order; orders/import importOne hits bexio with the OAuth token. None of these verify the Cf-Access-Jwt-Assertion header server-side. The only thing standing between the open internet and these mutations is Cloudflare Access at the edge plus a not-yet-confirmed Hetzner origin-IP firewall â€” cf-access.ts's own comment (L9-11) says origin-side verification is 'defense-in-depth' and the firewall 'becomes belt-and-suspenders' implying it is the intended boundary. The asymmetry is dangerous: a developer reasonably assumes the /api/* pattern (verify-in-code) is the project's auth model, but the page actions silently opt out.",
    "impact": "If the origin is ever reachable without Cloudflare Access in front (misconfigured DNS/proxy bypass, direct origin IP hit before the Hetzner firewall is in place, a temporarily-disabled CF Access app, or a future route added to the public hostname), an unauthenticated POST to /billing-runs?/retry with a subscription_id creates and emails a real invoice to a customer; a POST to /?/toggle can enable/disable billing on any order. Money movement with no in-code authorization.",
    "trigger": "Any request that reaches the origin without passing CF Access (origin-IP exposure, CF Access app disabled/misconfigured, or firewall not yet locked to CF IPs) can invoke the form actions directly.",
    "suggestedFix": "Add a `handle` hook in hooks.server.ts that calls verifyCfAccess(event.request) for all non-public routes (allow only /health), populating event.locals.identity and returning 401 on CfAccessError â€” making auth uniform across pages and APIs instead of per-endpoint and inconsistent.",
    "confidence": 0.85,
    "verifyNote": "Confirmed exactly as described. hooks.server.ts (lines 1-21) exports ONLY handleError; grep for `export const handle` across apps/web/src returns no match, so there is no global `handle` hook, and app.d.ts L7 leaves Locals empty. The three JSON endpoints each call verifyCfAccess (trigger-run/+server.ts:37, sync/+server.ts:18, runs/[id]/+server.ts:19). The page form actions do NOT: billing-runs/+page.server.ts `retry` (L30-41) calls getValidAccessToken + runSubscriptionNow() â€” the one action in the whole web app that creates AND sends a real invoice â€” with zero in-code auth; +page.server.ts `toggle` (L51-67) flips recurring_orders.enabled (gates billing) with zero auth; settings/+page.server.ts actions (L92-163) can flip auto_send_invoices, the mail template, due window, and webhook URL â€” all billing-affecting â€” with zero auth; orders/import importOne/syncNow use the OAuth token with zero auth. The harm is reachable only if a request reaches the origin without passing CF Access (origin-IP exposure, CF Access app disabled/misconfigured, or the Hetzner firewall not yet locked to CF IPs). cf-access.ts's own comment (L8-11) confirms the author treats origin-side JWT verification as a deliberate boundary (\"even if someone bypasses CF and hits the origin directly, requests ... are 401\") and notes the firewall \"becomes belt-and-suspenders\" â€” i.e., not yet confirmed in place. So the page actions, including the money-mover, opt out of the exact defense-in-depth layer the author built for the API endpoints. This is a real, code-grounded auth asymmetry, not a guarded non-issue. P1 (not P0) is correct: it is exploitable-but-gated behind Cloudflare Access at the edge; there is no unauthenticated path while CF Access correctly fronts the origin.",
    "refinedFix": "Add a `handle` hook in hooks.server.ts (export const handle: Handle) that, for every request except a small public allowlist, calls verifyCfAccess(event.request), populates event.locals.identity (and declares it in app.d.ts App.Locals), and returns a 401 Response on CfAccessError. The public allowlist must include /health (Coolify canary) and the bexio OAuth flow routes /callback and /auth/bexio (these are hit by bexio's redirect, not by an authenticated CF Access user, so blanket-verifying them would break OAuth). This makes auth uniform across pages and APIs and removes the per-endpoint inconsistency, so the highest-value mutation (billing-runs retry -> create+send invoice) is no longer protected solely by the edge. After adding the central hook, the per-endpoint verifyCfAccess calls in the /api/* +server.ts files become redundant but can stay as belt-and-suspenders."
  },
  {
    "title": "Dashboard 'today's invoices' query computes the day boundary in the DB session timezone (UTC), not Europe/Zurich",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/web/src/routes/+page.server.ts",
    "lines": "19-23",
    "description": "todayInvoicesPromise filters with `${invoiceRuns.sentAt}::date = current_date OR ${invoiceRuns.updatedAt}::date = current_date`. Casting a timestamptz to ::date and comparing to current_date both resolve in the Postgres session TimeZone. Per project notes the Coolify server runs UTC, so 'today' is a UTC calendar day, while the business operates in Europe/Zurich (UTC+1/+2) and billing_period keys are explicitly Europe/Zurich (schema.ts L102). Between 00:00 and 02:00 CH time, current_date in UTC is still 'yesterday', and invoices created/sent late evening CH (after 22:00/23:00 UTC roll-over) land on the next UTC day.",
    "impact": "The dashboard 'Heute' panel shows the wrong day's invoice activity for a ~1-2h window around midnight CH, and the daily 08:00 CH cron run's own invoices could momentarily be attributed to the wrong calendar day. Cosmetic/observability only â€” no billing effect.",
    "trigger": "Viewing the dashboard in the early-morning CH window, or invoices sent late in the CH evening that cross the UTC date line.",
    "suggestedFix": "Compute the boundary in Zurich time, e.g. `(${invoiceRuns.sentAt} AT TIME ZONE 'Europe/Zurich')::date = (now() AT TIME ZONE 'Europe/Zurich')::date`, or set the connection's timezone explicitly.",
    "confidence": 0.85,
    "verifyNote": "Confirmed in current code. apps/web/src/routes/+page.server.ts L22 filters with `${invoiceRuns.sentAt}::date = current_date OR ${invoiceRuns.updatedAt}::date = current_date`. Both `::date` casts of a timestamptz and `current_date` resolve in the Postgres session TimeZone. The postgres.js client in packages/db/src/index.ts (L19-23) is created with NO `connection: { timezone: ... }` option and issues no `SET TIME ZONE` â€” so the session inherits the server/role default. Per CLAUDE.md and project memory the Coolify Postgres server runs UTC, so \"today\" is a UTC calendar day. The business is Europe/Zurich (UTC+1/+2), and the schema doc (schema.ts L101-108) explicitly notes billing_period keys are Europe/Zurich, confirming the intended business calendar is CH-local. Reachability: in CEST (UTC+2), a CH calendar day spans 22:00 UTC (prev) to 22:00 UTC; between 00:00â€“02:00 CH (22:00â€“00:00 UTC prev), current_date in UTC is still yesterday, and invoice activity at 22:00â€“23:59 CH (â‰ˆ20:00â€“21:59 UTC) vs after maps onto adjacent UTC dates â€” so the \"Heute\" panel shows the wrong day's rows for a ~1-2h window straddling CH midnight. No existing guard neutralizes it (no session-tz set, no AT TIME ZONE in the query). Harm is strictly cosmetic/observability: the panel is display-only and does not drive billing, idempotency, or due-gating (those use billing_period keys and isOrderDue, computed separately). The daily 08:00 CH cron runs at 06:00/07:00 UTC, far from any date boundary, so cron-created invoices are correctly attributed when viewed during business hours; only late-evening-CH manual/edge activity or early-morning viewing is affected. P3 is correct â€” no money or state impact.",
    "refinedFix": "Compute the boundary in Zurich time so it matches the business calendar and the billing_period keys: `(${invoiceRuns.sentAt} AT TIME ZONE 'Europe/Zurich')::date = (now() AT TIME ZONE 'Europe/Zurich')::date OR (${invoiceRuns.updatedAt} AT TIME ZONE 'Europe/Zurich')::date = (now() AT TIME ZONE 'Europe/Zurich')::date`. Note `AT TIME ZONE 'Europe/Zurich'` on a timestamptz yields a timestamp (no tz) already shifted to CH wall-clock, so the `::date` then reflects the CH calendar day; `now() AT TIME ZONE 'Europe/Zurich'` gives the current CH wall-clock to derive 'today'. Alternatively set `connection: { timezone: 'Europe/Zurich' }` on the postgres.js client in packages/db/src/index.ts, but that is a broader change affecting every query and the worker too, so the per-query AT TIME ZONE approach is safer and more localized."
  },
  {
    "title": "Dashboard and import loaders run unbounded SELECTs with no LIMIT",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/web/src/routes/+page.server.ts",
    "lines": "19-30",
    "description": "ordersPromise selects ALL recurring_orders and todayInvoicesPromise selects all invoice_runs touched today, with no LIMIT; orders/import/+page.server.ts L29 selects all recurring_orders; subscriptions/+page.server.ts L11-14 selects all subscriptions plus a DISTINCT ON over the entire billing_runs table. The file comment acknowledges the cap ('works comfortably up to ~1k rows; switch to server-side pagination if we ever cross that'). settings.ts getSettings() (L29-36) also reads every app_settings row and filters in JS rather than using an IN clause. For the current solo-tenant scale this is benign, but billing_runs grows unbounded over time (one row per subscription per period) and the DISTINCT ON scan over all of it has no upper bound.",
    "impact": "As billing_runs and invoice_runs accumulate over months/years, page loads scan ever-larger tables; eventually slow dashboard loads and increased DB load. No correctness impact.",
    "trigger": "Long-running production with accumulated billing_runs/invoice_runs history.",
    "suggestedFix": "Add LIMIT + server-side pagination to the orders/subscriptions/today loaders, and make getSettings() filter with a WHERE key IN (...) instead of full-table scan + JS filter.",
    "confidence": 0.7,
    "verifyNote": "Code matches the description exactly. apps/web/src/routes/+page.server.ts L19-30: todayInvoicesPromise (L19-23) and ordersPromise (L27-30) both run unbounded SELECTs with no LIMIT. orders/import/+page.server.ts L29 selects all recurring_orders. subscriptions/+page.server.ts L11-14 selects all subscriptions and L18-27 runs a raw DISTINCT ON over the entire billing_runs table with no bound. packages/db/src/settings.ts getSettings() L29-36 does select-all + JS Set filter instead of WHERE key IN (...). All claims verified true. However, the harm is purely a long-term scalability/performance footnote with ZERO correctness, money, or security impact (the finding itself concedes 'No correctness impact'). These are read-only dashboard loaders behind Cloudflare Access for a single solo-tenant operator. recurring_orders/subscriptions/app_settings are naturally bounded (dozens of orders, handful of config keys) â€” the getSettings full-scan over a tiny config table is a non-issue. Only billing_runs/invoice_runs grow monotonically (one row per period), and even there a DISTINCT ON full scan over tens of thousands of rows is sub-millisecond-to-low-ms in Postgres; degradation is years away, gradual, and already self-documented in the file comment ('works comfortably up to ~1k rows; switch to server-side pagination if we ever cross that') as a known future trigger. Real but bottom-of-P3 polish/hardening; not P2 (no money-detection delay, no stuck state, no data loss).",
    "refinedFix": "Lowest-priority of the set: add a covering index on billing_runs(subscription_id, created_at DESC) so the DISTINCT ON stays index-only as the table grows â€” cheaper and more durable than refactoring the loaders. Defer LIMIT/pagination on the orders/subscriptions/today loaders until the in-code ~1k threshold is actually approached. The getSettings() WHERE key IN (...) change is cosmetic â€” app_settings is a tiny config table â€” and can be skipped."
  },
  {
    "title": "retryIssuedRows re-sends an already-mailed invoice (duplicate customer email) â€” no is_sent guard before re-send",
    "severity": "P1",
    "category": "bug",
    "file": "apps/worker/src/lib/state-machine.ts",
    "lines": "636-644",
    "description": "retryIssuedRows() picks up every row in status='issued' and unconditionally calls sendInvoice() again. It does call getInvoice() at line 637, but ONLY to read live.document_nr â€” it never inspects live.is_sent / live.mail_sent_at before re-sending. This is the asymmetry with reconcileInFlightSends(), which DOES check is_sent (lines 553, 560) precisely to avoid double-mailing. The path that lands a row back in 'issued' after a real send is processOrder's catch block: sendInvoice() succeeds (lines 425-430) and the email goes out, then transitionTo('sent') at line 432 (or its DB write) fails â€” connection drop, pool timeout, deadlock. Because wasIssued=true, the catch at line 445 rolls the row back to status='issued' (line 455) instead of 'failed'. On the next run retryIssuedRows finds that 'issued' row and re-sends the same invoice.",
    "impact": "The customer receives a duplicate invoice email for the same period. Reputational damage and confusion; in a financial bot this looks like a double-billing demand even though only one invoice exists. Repeats once per run until attempts reaches MAX_ATTEMPTS (up to 2 extra emails).",
    "trigger": "sendInvoice succeeds but the immediately-following 'sent' DB transition fails (transient Postgres/pool error after the bexio mail call). Realistic under load or a brief DB blip, which is exactly when the 1.1s-paced long run is most likely to hit a connection hiccup.",
    "suggestedFix": "In retryIssuedRows, after getInvoice() check live.is_sent || live.mail_sent_at; if already sent, transition the row straight to 'sent' (reconcile) and skip sendInvoice â€” mirror the guard already present in reconcileInFlightSends (lines 553-559).",
    "confidence": 0.85,
    "verifyNote": "Confirmed real and reachable in current code. retryIssuedRows (state-machine.ts:636-644) fetches `live` via getInvoice at L637 but reads ONLY live.document_nr at L638; it then calls sendInvoice at L639 with NO check of live.is_sent / live.mail_sent_at. The asymmetry with reconcileInFlightSends is real: that function DOES guard on `live.is_sent || live.mail_sent_at` (L553) and on an attempts>=1 heuristic (L560) precisely to avoid duplicate mailing. The trigger path is reachable: in processOrder, sendInvoice succeeds (L425-430, email already mailed), then transitionTo('sent') at L432 throws on a transient DB error; the catch at L438 sees wasIssued=true (set L412) and sets status='issued', lockAcquiredAt=null (L453-456) â€” NOT 'failed'. reconcileInFlightSends only scans status='sending' (L535), so it never intercepts this row; only retryIssuedRows handles 'issued' rows, and it re-sends unconditionally. The claim row starts attempts=1 (L219) and retryIssuedRows re-claims with lt(attempts, MAX_ATTEMPTS) and increments, so it re-mails on subsequent runs until attempts hits MAX_ATTEMPTS â€” a duplicate invoice email to a real customer. BexioInvoice exposes is_sent (types.ts:219) and mail_sent_at (types.ts:220), so the guard is implementable. Net effect: no second invoice is created (same invoiceId is re-mailed), so this is not money-loss/double-billing P0, but it is a duplicate billing-demand email to a real customer on a realistic transient post-send DB blip â€” the reputational P1 class. Not in the KNOWN-ALREADY-FIXED list (N-5 covers reconcileInFlightSends' assume-sent heuristic, NOT retryIssuedRows, which lacks any such guard).",
    "refinedFix": "The suggested fix (add `if (live.is_sent || live.mail_sent_at) -> transition to 'sent' and skip send`) is correct but INSUFFICIENT alone, because the codebase's own documented bexio read-back quirk (invoices.ts:182-184; state-machine.ts:560-569) means is_sent frequently stays undefined/false even after a real send â€” exactly in this post-send window. To actually prevent the duplicate, retryIssuedRows must fully mirror reconcileInFlightSends' logic: after getInvoice, (1) if live.is_sent||live.mail_sent_at -> reconcile to 'sent', skip send; (2) ELSE if row.attempts (already-incremented) indicates a prior send attempt (>=1, i.e. this row was previously claimed and may have mailed) -> assume sent, reconcile to 'sent', skip send, log the bexio read-back caveat; only actually call sendInvoice when there is no evidence of a prior send. Better still: distinguish 'issued-after-create, never sent' from 'issued-after-send-failed-to-record' with a dedicated status or flag set in processOrder's catch (e.g. a `mailAttempted` boolean), so retryIssuedRows knows whether sendInvoice already ran rather than inferring from the flaky is_sent field."
  },
  {
    "title": "reconcileInFlightSends assumes-sent for a row that crashed BEFORE /send was ever called (attempts pre-set to 1 at claim)",
    "severity": "P2",
    "category": "bug",
    "file": "apps/worker/src/lib/state-machine.ts",
    "lines": "560-574",
    "description": "The claim INSERT in processOrder sets attempts: 1 up-front (line 219), before any bexio call. The branch order inside reconcileInFlightSends checks `(row.attempts ?? 0) >= 1` (line 560) BEFORE the `attempts === 0` retry branch (line 578). So a row that reached status='sending' (transitionTo at line 422) and then crashed BEFORE sendInvoice() executed (line 425) is reconciled with attempts=1, hits the >=1 assume-sent branch, and is marked 'sent' even though the email never went out. The 'attempts === 0 â†’ safe to retry' branch at line 578 is therefore unreachable for the order pipeline, because attempts is never 0 for a real claimed row â€” it is 1 from creation. (The confirmed finding frames this as 'attempts set at claim-time'; the distinct mechanical gap is that the intended attempts===0 retry path can never fire, so NO crashed-before-send row is ever retried â€” it is silently swallowed as 'sent'.)",
    "impact": "An invoice that was issued (festgeschrieben in bexio, so it counts as revenue owed) but whose email crashed before transmission is marked 'sent' and never mailed. Money is never collected and no error surfaces â€” the run reports a successful send.",
    "trigger": "Process crashes (OOM kill, Coolify redeploy, container restart) in the narrow window after the 'sending' transition and before/at the sendInvoice network call. The daily long run with 1.1s pacing keeps this window open for the whole run.",
    "suggestedFix": "Track send-attempt count separately from claim-existence: do not pre-set attempts=1 at claim; increment attempts only immediately before calling sendInvoice. Then the attempts===0 branch correctly means 'never tried' and reconcile will retry instead of assume-sent.",
    "confidence": 0.78,
    "verifyNote": "CONFIRMED reachable in current code. processOrder's claim INSERT hard-codes attempts:1 (state-machine.ts:219), and transitionTo never touches attempts (verified lines 490-498: it only sets status/updatedAt/issuedAt/sentAt/lockAcquiredAt). So a row that commits the 'sending'+lockAcquiredAt transition (line 422-424) and then crashes during the sendInvoice network call (line 425) sits in status='sending' with attempts=1. In reconcileInFlightSends the branch order is: is_sent (553) â†’ attempts>=1 assume-sent (560) â†’ attempts>=MAX (575) â†’ else attempts===0 retry (578). Because order rows are never attempts=0, the >=1 assume-sent branch fires first and the attempts===0 retry branch (578-590) is dead code for the only caller. I confirmed via grep that subscriptions.ts does not use reconcileInFlightSends and the invoiceRuns attempts column, so the order pipeline is the sole consumer â€” the retry path is genuinely unreachable. The functional consequence is real: a row that crashed BEFORE bexio ever received the /send gets marked 'sent' (line 570) even though no email went out, and the run reports success. The N-5 comment's justification ('we ALREADY tried at least once', lines 561-566) is factually false for this window â€” the claim-time attempts=1 conflates 'row claimed' with 'send attempted'. This is a NEW mechanical gap beyond the documented N-5 assume-sent tradeoff (which assumed a real prior send attempt). Why P2 not P1: the invoice is festgeschrieben and persists in bexio (recoverable â€” Marcus can resend from the bexio UI), only the email delivery is missed, and the trigger is a narrow sub-second-to-second crash window during sendInvoice. It is incorrect-but-recoverable, not money-lost/double-billed. The N-5 design also already provides a manual-recovery fallback (missing-receipt â†’ re-trigger). Note one nuance the finding overstates: a crash AFTER bexio processed the send (but before our process recorded it) is reconciled CORRECTLY as sent â€” so not every crash-in-window is a missed email, only those that crash before bexio receives the /send.",
    "refinedFix": "Decouple send-attempt count from claim existence. At claim INSERT (line 219) set attempts:0 instead of 1, and increment attempts immediately BEFORE calling sendInvoice (i.e., bump in the 'sending' transition at line 422 via a dedicated attempts++ or in retryIssuedRows-style claim). Then attempts===1+ at reconcile genuinely means 'send was attempted', the assume-sent branch (560) only fires for rows that actually reached the network call, and the attempts===0 branch (578) correctly retries crash-before-send rows. Verify the snapshot reuse path (lines 279-291) and retryIssuedRows (606-616) still agree on the new attempts semantics. Lower-effort alternative: in reconcileInFlightSends, gate the assume-sent branch on a separate 'sendStartedAt' timestamp (set right before line 425) rather than on attempts, so crash-before-send is distinguishable from crash-after-send."
  },
  {
    "title": "validateSubscriptionInputs rejects legitimate negative/credit positions and zero-price free items as 'zero sale_price'",
    "severity": "P3",
    "category": "edge-case",
    "file": "apps/worker/src/lib/subscriptions.ts",
    "lines": "76-84",
    "description": "validateSubscriptionInputs throws if any article.sale_price is null OR Number(price) === 0. Number(price) === 0 is also true for a price string that fails to parse (Number('CHF 5') === NaN is NOT 0, but Number('') === 0 and Number(' ') === 0), and it hard-blocks a deliberately CHF 0 line item (e.g. a bundled free article or a 100%-discounted promo position) that a real subscription may legitimately contain alongside paid items. Combined with runSubscriptionNow still being reachable from the dashboard, this throws keep-failed and marks the whole billing_run failed.",
    "impact": "A subscription containing one valid free/zero-priced item alongside paid items can never bill â€” it is permanently stuck 'failed' on every attempt, blocking all its revenue, with a misleading 'missing or zero sale_price' message.",
    "trigger": "A subscription whose item list includes a CHF 0.00 article (free bundled component) â€” common in real product catalogs. processOneSubscription throws before creating the invoice.",
    "suggestedFix": "Validate per-invoice total > 0 rather than per-article price !== 0, and treat null/NaN sale_price (unparseable) distinctly from an intentional 0.00 line. Only fail when the assembled invoice total would be zero.",
    "confidence": 0.62,
    "verifyNote": "Mechanism is confirmed in current code. validateSubscriptionInputs (apps/worker/src/lib/subscriptions.ts:76-84) loops over every article and throws a plain Error when `price == null || Number(price) === 0`. A plain Error is classified `keep-failed` by classifyForRetry (lines 97-104, only BexioApiError transient/rate_limit gets delete-and-retry), so the billing_run is set status='failed' and persists (lines 289-296), and next_billing_date never advances â€” the subscription re-fails every cron run. runSubscriptionNow is genuinely reachable from the dashboard (apps/web/src/routes/subscriptions/[id]/+page.server.ts:99 and billing-runs/+page.server.ts:39), and the daily cron reaches the same code via processSubscriptions/processOneSubscription. So a multi-item subscription containing one intentional CHF 0.00 line alongside paid lines is permanently blocked with a misleading \"missing or zero sale_price\" message, even though the assembled invoice total (built at lines 205-214) would be positive and legitimate. The genuine defect is granularity: the guard checks per-article price !== 0 instead of per-invoice total > 0, conflating the intended N-8 case (sale_price == null, truly unconfigured) with a deliberate 0.00 promo/bundle line.\n\nHowever, severity is overstated as P2. (1) The zero-price fail-closed was a deliberate, tested design decision (N-8, with explicit tests in subscriptions.test.ts) â€” for a single free item, blocking a CHF 0 invoice is the intended and arguably correct behavior. (2) The only real gap is the mixed paid+free multi-line subscription, which requires a specific catalog setup that is not the default and for which there is zero evidence in this single-tenant deployment (Marcus's own recurring orders/canary). No money is lost, nothing double-bills, no wrong amount is sent â€” at worst a niche-but-legitimate configuration is refused and surfaces as a 'failed' run that the operator sees on the dashboard. The Number('')/Number(' ') sub-claim is a minor, non-load-bearing aside (bexio returns numeric strings, so empty/whitespace is unrealistic). Net: a real, correctly-code-grounded hardening opportunity, but defense-in-depth / edge-case, hence P3.",
    "refinedFix": "Replace the per-article `Number(price) === 0` rejection with two distinct checks: (1) keep the null/NaN guard â€” fail when sale_price is null or `Number.isNaN(Number(price))` (truly unconfigured price, the original N-8 intent); (2) compute the assembled invoice total from positions (sum of qty * unit_price across all items) and fail only when that total is <= 0. This permits a deliberate CHF 0.00 line item to coexist with paid lines while still fail-closing a zero/empty-total invoice. Update the misleading error message to distinguish \"unconfigured price\" from \"zero invoice total\", and add a test for a mixed paid+free multi-item subscription."
  },
  {
    "title": "Token-refresh error body (may contain refresh token) is logged/propagated verbatim into BexioApiError and persisted to error_jsonb",
    "severity": "P3",
    "category": "security",
    "file": "packages/bexio-client/src/auth.ts",
    "lines": "94-97",
    "description": "On a failed token refresh, the full response body is interpolated into the thrown BexioApiError message: `Token refresh failed: ${body}`. That error propagates up through runDaily and trigger-run, where it is written to bot_runs.errorsJsonb (run.ts lines 100-103) and console.error'd. OAuth IdP error responses can echo back request parameters or token fragments. error_jsonb is rendered on the dashboard /runs page and bot_runs is included in the daily pg_dumpall to S3, so a sensitive IdP body would be persisted and surfaced.",
    "impact": "A refresh-token fragment or client_secret echoed in an IdP error response would be persisted to the DB, the S3 backup, and the dashboard â€” a credential leak into lower-trust storage. Also the hard-coded errorClass='auth' (confirmed elsewhere) means a transient 5xx from the IdP also dumps its body here.",
    "trigger": "bexio's auth server returns a verbose 400/500 error body that includes echoed request params during a refresh failure (e.g. invalid_grant responses sometimes echo the token).",
    "suggestedFix": "Truncate and redact the token body before embedding it in the error message; log only status + error code class, not the raw IdP body. At minimum slice() and strip anything resembling a token before persisting to error_jsonb.",
    "confidence": 0.55,
    "verifyNote": "PARTIALLY CONFIRMED, with two of the finding's claims overstated. Verified code: auth.ts:94-97 interpolates the raw IdP `body` into the 3rd BexioApiError constructor arg. BexioApiError (types.ts:300-310) sets `.message = `bexio API ${status} (auth): ${(\"Token refresh failed: \"+body).slice(0,200)}`` â€” TRUNCATED to 200 chars; the full untruncated body lives only in `.body`. The persisted path (run.ts:124-127 â†’ :159, and trigger-run/+server.ts:94,101) writes `err.message` (the truncated 200-char string), NOT `.body`. So only a ~178-char fragment of the IdP body can reach bot_runs.errorsJsonb; the \"full body persisted\" framing is already mitigated by the constructor's slice(0,200). CLAIM REFUTED: \"error_jsonb is rendered on the dashboard /runs page\" is false â€” /runs/+page.svelte (verified) only shows a 'mit Fehlern' badge (line 35); errorsJsonb content is never rendered, and the page's `data.errors` block (lines 144-150) is query-rejection text from the loader (+page.server.ts:46-49), unrelated to botRuns.errorsJsonb. CLAIM WEAK: bexio auth is Keycloak (http.ts:8: auth.bexio.com/realms/bexio); Keycloak token errors follow RFC 6749 ({\"error\":\"invalid_grant\",\"error_description\":...}) and do not echo back refresh_token/client_secret, so the 'token fragment echoed' trigger is speculative. Net: real but minor â€” a truncated IdP error_description fragment can persist into DB/S3 (lower-trust storage). Genuine defense-in-depth, correctly P3; the 'credential leak' and 'rendered on dashboard' framings are overstated.",
    "refinedFix": "In auth.ts:95-96, do not embed the raw IdP body verbatim. Parse the response as form/JSON and surface only the OAuth `error` code (e.g. `invalid_grant`) plus a short redacted `error_description` slice: `throw new BexioApiError(res.status, 'auth', `Token refresh failed: ${oauthErrorCode ?? '<no error code>'}`)`. Keep the raw body for transient/log-only diagnostics via console at most, never into the BexioApiError.body that flows to error storage. This is hardening, not urgent â€” the message is already truncated to 200 chars and Keycloak does not echo credentials."
  }
]
